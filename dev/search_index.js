var documenterSearchIndex = {"docs":
[{"category":"section","location":"branchable/#The-branchable-world-interface","page":"The branchable-world interface","text":"The weak-derivative branching estimator (branching_gradient) cannot run from a recorded trajectory: forcing a firing changes which clocks are subsequently enabled, so only a running world can continue the counterfactual. But the estimator does not need any particular framework — it needs nine abstract capabilities. This page states those capabilities as a protocol, shows how a framework adopts it with a complete worked example built directly on the raw CompetingClocks sampler layer (no ChronoSim anywhere), and shows how check_branchable certifies an implementation.\n\nThe protocol is duck-typed on purpose: a world is branchable because the nine generic functions have methods for its type, not because it subtypes an abstract supertype. A foreign framework's simulation type already has a supertype of its own, and Julia types cannot be re-parented retroactively — an abstract BranchableWorld would exclude exactly the adopters the protocol exists for. A world missing a verb fails at first use with an ordinary MethodError naming the missing generic.","title":"The branchable-world interface"},{"category":"section","location":"branchable/#The-nine-verbs-and-their-obligations","page":"The branchable-world interface","text":"Each verb's docstring (see the API reference) is the normative statement; this table is the map. K is the framework's clock-key type.\n\nVerb Returns Semantic obligation\nbranch_peek(w) (t, key) or nothing Non-committing and repeatable: two peeks with no intervening mutation agree and perturb nothing.\nbranch_commit!(w, key, t) — Fire the peeked reservation through the framework's normal update path; time advances to exactly t.\nbranch_force!(w, key, t) — Fire the CHOSEN enabled clock at t through the SAME update path as a natural firing; t must be the current race's decision time (keep-if-later precondition).\nbranch_clone(w) w′ Coupled full copy: with no rekey, the clone's peek/commit future is identical to the original's; cloning perturbs the original not at all.\nbranch_rekey!(w, seed) — Fresh randomness derived from seed, INCLUDING a resample of already-scheduled firings (at the current time, a stopping time); same-seed rekeys of two clones couple to each other.\nbranch_time(w) Float64 Current simulation time.\nbranch_enabled_ages(w) Vector{Tuple{K,Float64}} Every enabled clock with its age at the current time, SORTED BY KEY — the Hahn–Jordan pmf indexes this order.\nbranch_clock_distribution(w, θ, key) UnivariateDistribution The named enabled clock's lifetime distribution rebuilt at θ (possibly dual-valued); the world supplies state context internally.\nbranch_state(w) any The object handed to the user's f_state terminal functional.\n\nTwo obligations deserve emphasis because naive implementations violate them:\n\nSorted ages are load-bearing. The estimator builds the who-fires-next probability vector in branch_enabled_ages order and indexes its Hahn–Jordan draws back into that order; two coupled worlds must present the same clocks in the same positions.\nRekeying must redraw scheduled clocks. A scheduling backend caches putative firing times, so re-seeding the streams alone would leave the world replaying its pre-rekey draws — branching_gradient rekeys each replication's factory world, and without the redraw every replication would share the factory's opening firings. The redraw is a resample at a stopping time (each clock's remaining lifetime, conditioned on its age), so the trajectory law is untouched. CompetingClocks.jitter! is exactly this operation: it redraws every scheduled entry from its own freshly-keyed stream using the clock's stored distribution, and on CombinedNextReaction it also extinguishes the retained-disabled survival banks, residual randomness that a sweep over the enabled clocks alone could not reach. It is the same primitive CompetingClocks' own split! pairs with rekey_streams!.\n\nChronoSim's SimulationFSM conforms through the ClockGradients–ChronoSim package extension, which maps each verb onto a public ChronoSim or CompetingClocks capability (next, fire!, force_fire!, clone, rekey_streams! paired with jitter!, enabled_ages, and the model's four-argument enable seam). The extension also keeps the CG-M4 calling convention branching_gradient(sim_factory, initializer, θ, f_state; ...) working: it wraps the pair into a conforming world factory and forwards.","title":"The nine verbs and their obligations"},{"category":"section","location":"branchable/#A-ready-made-world:-ClockWorld","page":"The branchable-world interface","text":"Most users never need to implement the verbs. The package ships ClockWorld, a minimal simulation runner (model-contract state + CombinedNextReaction sampler + current time) that implements every verb, so any pure five-function model runs the clone-based estimators with one constructor:\n\nw = ClockWorld(model, θ; seed=1)                       # a ready-to-peek world\nbranching_gradient(() -> ClockWorld(model, θ; seed=1), θ, f_state;\n                   nreps=2000, horizon=8.0, seed=42, branch_rng_seed=43)\n\nIts one limitation is stated on the type: clock distributions are frozen at enabling, so it is exact only for models whose enabled clocks' laws cannot change while the clock stays enabled. A framework world (the ChronoSim extension) re-evaluates mid-flight.","title":"A ready-made world: ClockWorld"},{"category":"section","location":"branchable/#Adopting-the-protocol:-a-world-with-no-framework-at-all","page":"The branchable-world interface","text":"The proof that the protocol suffices is a world built straight on the raw sampler layer — the stand-in for, say, a queueing package that has never heard of ChronoSim. The state is a model-contract state (immutable, copied on fire), the clocks live in a CombinedNextReaction, and each verb is a few lines. This is a compact version of the packaged ClockWorld (src/clockworld.jl), whose full form drives the machine-repair CTMC-oracle test in the test suite.\n\nusing ClockGradients\nusing CompetingClocks: CombinedNextReaction, enable!, disable!, fire!, next,\n    clone, rekey_streams!, jitter!, force_fire!, enabled_ages\nusing Distributions\nusing Random: Xoshiro\n\nimport ClockGradients: initial_state, clockkeytype, enabled, clock_distribution, fire\nimport ClockGradients: branch_peek, branch_commit!, branch_force!, branch_clone,\n    branch_rekey!, branch_time, branch_enabled_ages, branch_clock_distribution,\n    branch_state\n\n# The model, in the package's five-function contract: two machines that fail\n# (rate λ = θ[1]) and one repairman (rate μ = θ[2]), with a failure counter\n# carried in the state so the count is a terminal-state functional.\nstruct TwoMachines end\nstruct TMState\n    up::Tuple{Bool,Bool}\n    nfail::Int\nend\ninitial_state(::TwoMachines) = TMState((true, true), 0)\nclockkeytype(::TwoMachines) = Tuple{Symbol,Int}\nfunction enabled(::TwoMachines, s::TMState)\n    ks = Tuple{Symbol,Int}[]\n    s.up[1] && push!(ks, (:fail, 1))\n    s.up[2] && push!(ks, (:fail, 2))\n    (s.up[1] && s.up[2]) || push!(ks, (:repair, !s.up[1] ? 1 : 2))\n    ks\nend\nclock_distribution(::TwoMachines, θ, key) =\n    key[1] === :fail ? Exponential(one(eltype(θ)) / θ[1]) :\n                       Exponential(one(eltype(θ)) / θ[2])\nfunction fire(::TwoMachines, s::TMState, key)\n    up = collect(s.up)\n    key[1] === :fail ? (up[key[2]] = false) : (up[key[2]] = true)\n    TMState((up[1], up[2]), s.nfail + (key[1] === :fail))\nend\n\n# The world: state + sampler + clock, and the nine verbs.\nmutable struct MiniWorld\n    const model::TwoMachines\n    const θ::Vector{Float64}\n    state::TMState\n    sampler::CombinedNextReaction{Tuple{Symbol,Int},Float64}\n    time::Float64\nend\nfunction MiniWorld(θ; seed)\n    sampler = CombinedNextReaction{Tuple{Symbol,Int},Float64}(UInt64(seed))\n    state = initial_state(TwoMachines())\n    w = MiniWorld(TwoMachines(), collect(float.(θ)), state, sampler, 0.0)\n    for k in enabled(w.model, state)\n        enable!(sampler, k, clock_distribution(w.model, w.θ, k), 0.0, 0.0)\n    end\n    w\nend\n\n# GSMP retention shared by commit and force: survivors keep their draws, the\n# fired clock (and any newly enabled key) starts fresh at the firing time.\nfunction apply_firing!(w::MiniWorld, key, t)\n    old = enabled(w.model, w.state)\n    w.state = fire(w.model, w.state, key)\n    new = enabled(w.model, w.state)\n    for k in old\n        k == key && continue\n        k in new || disable!(w.sampler, k, t)\n    end\n    for k in new\n        (k == key || !(k in old)) &&\n            enable!(w.sampler, k, clock_distribution(w.model, w.θ, k), t, t)\n    end\n    w.time = t\n    w\nend\n\nfunction branch_peek(w::MiniWorld)\n    (t, k) = next(w.sampler, w.time)\n    (k === nothing || !isfinite(t)) ? nothing : (t, k)\nend\nbranch_commit!(w::MiniWorld, key, t) = (fire!(w.sampler, key, t); apply_firing!(w, key, t))\nbranch_force!(w::MiniWorld, key, t) = (force_fire!(w.sampler, key, t); apply_firing!(w, key, t))\nbranch_clone(w::MiniWorld) = MiniWorld(w.model, copy(w.θ), w.state, clone(w.sampler), w.time)\nfunction branch_rekey!(w::MiniWorld, seed)\n    rekey_streams!(w.sampler, UInt64(seed))\n    jitter!(w.sampler, w.time)   # redraw every scheduled clock, conditioned on age\n    w\nend\nbranch_time(w::MiniWorld) = w.time\nbranch_enabled_ages(w::MiniWorld) = enabled_ages(w.sampler, w.time)\nbranch_clock_distribution(w::MiniWorld, θ::AbstractVector, key) =\n    clock_distribution(w.model, θ, key)\nbranch_state(w::MiniWorld) = w.state\nnothing # hide","title":"Adopting the protocol: a world with no framework at all"},{"category":"section","location":"branchable/#Certifying-with-check_branchable","page":"The branchable-world interface","text":"The conformance harness exercises the obligations the signatures cannot express and returns one boolean per obligation plus diagnostics, rather than throwing — assert on it in the adopting package's test suite.\n\nθ = [0.5, 1.5]\nreport = check_branchable(() -> MiniWorld(θ; seed=1), θ; nsteps=20, seed=0xBEEF)\n\nEvery field must be true; a failed check names itself in report.diagnostics. The package's own suite also runs negative controls — a world whose peek secretly advances the trajectory, and one that reports its ages out of key order — and asserts the corresponding check catches each.","title":"Certifying with check_branchable"},{"category":"section","location":"branchable/#Branching-through-the-conforming-world","page":"The branchable-world interface","text":"With conformance certified, branching_gradient runs unchanged:\n\nres = branching_gradient(() -> MiniWorld(θ; seed=1), θ,\n                         s -> Float64(s.nfail);\n                         nreps=300, horizon=8.0, seed=2026, branch_rng_seed=7)\n(estimate = res.estimate, stderr = res.stderr, clones_per_rep = res.clones_per_rep)\n\nThe package's exit criterion for this interface is the same run at full strength on the five-machine model: the packaged ClockWorld implements these verbs over the shared machine-repair fixture, and at 800 replications the estimate [11.04 ± 0.68, 3.62 ± 0.18] matches the differentiated CTMC oracle [10.727, 3.568] at z = [0.46, 0.26] — the branching estimator reproducing its oracle through a world that has never heard of ChronoSim, which is the proof that the estimator depends only on this protocol.","title":"Branching through the conforming world"},{"category":"section","location":"branchable/#Deriving-the-contract-from-a-ChronoSim-model","page":"The branchable-world interface","text":"The branchable-world verbs above adapt a running ChronoSim simulation. The same package extension also derives the five-function model contract for a ChronoSim.GsmpModel — the model value holding the event families, the initial law, and the parameter names — so the record-replay estimators (score_estimate, ipa_estimate, paired_estimate) consume a ChronoSim model directly, with no hand-written parallel model. Each contract function is derived from the model value's own machinery: initial_state samples the point-mass initial law, clockkeytype is the model's instance-key union, enabled is a from-scratch generator scan sorted by the model's key order, the four-argument clock_distribution is the model's own enable(event, physical, θ, when) seam through the family's resolved parameter view, and fire clones the state and applies the engine's composite firing step (user fire! plus the immediate-event cascade).\n\nusing ClockGradients, ChronoSim   # loading both activates the extension\n\nmodel = ChronoSim.GsmpModel(\n    events  = (Fail, Repair),            # ordinary ChronoSim event types\n    initial = () -> all_up_state(5),     # a point-mass (deterministic) law\n    params  = (:lambda, :mu),\n)\nθ = [0.5, 1.5]\n\n# Simulate through ChronoSim's front door, ingest each MinimalRecord through\n# the derived contract, and run the paired score/IPA estimator unchanged.\nfn = IntegratedOccupancy(s -> count(!s.machine[i].up for i in eachindex(s.machine)))\nres = paired_simulate_and_estimate(rng, model, θ, fn; nreps=8000, horizon=8.0)\n\nThe conversion seam is gradient_record: it maps a ChronoSim.MinimalRecord's (key, time) firing sequence onto the bare-trace GradientRecord constructor, which reconstructs the enabling times, the retained log-uniforms, and the segment chains by folding the derived contract over the trace.\n\nBoth packages export functional types with the same three names (IntegratedOccupancy, TerminalObservable, FirstPassageTime) and the same constructor semantics — deliberately duplicated, because the dependency points ClockGradients → ChronoSim, so ChronoSim cannot import ClockGradients' types. When both packages are loaded, the extension accepts either package's functionals at the estimator entry points (score_estimate, ipa_estimate, paired_estimate, paired_simulate_and_estimate): a ChronoSim functional is converted to its ClockGradients twin on the way in, so a model written entirely against ChronoSim's types needs no qualification or hand conversion. The duplication itself remains; only the seam is closed (decision gate G-A, closed 2026-07-12).\n\nThe derivation refuses, by name, what it cannot make correct. The two refusals every adopter meets first:\n\nA random or θ-dependent initial law. The contract folds every trajectory's states from the one state initial_state(model) returns, so a law with more than one possible time-zero state has no single state to return. initial_state throws an ArgumentError naming the law's form and the fix: score such a model through ChronoSim's trace_likelihood (each MinimalRecord carries its own realized initial state), or declare the initial condition as a state value or a zero-argument thunk.\nA fire! that draws randomness. The derived fire must be a pure function of (state, key) — the state fold that reconstructs a record's trajectory has no seed to reproduce draws from. fire runs the composite step under a counting RNG and throws an ArgumentError naming the event type if any draw occurred; gradient_record likewise refuses a MinimalRecord whose fire_random flag is set. The fix is to move the draw into competing clocks (one event per outcome).\n\nThree further refusals guard the bookkeeping: a :resume memory policy (the record replay applies the GSMP fresh-clock rule), a delayed enabling (enable returning te ≠ when has no slot in the reconstructed ages), and the three-argument clock_distribution form (a GsmpModel's enables genuinely need the physical state, so only the state-dependent four-argument form is defined).","title":"Deriving the contract from a ChronoSim model"},{"category":"section","location":"branchable/#Capability-tiers","page":"The branchable-world interface","text":"Gradient technology has not caught up to full modern simulation expressiveness, and the framework's response is graded rather than prohibitive: nothing an author can write is ever refused at simulation time; each estimator states, per model and by name, exactly where the frontier sits. The grading is a ladder of tiers:\n\nTier 0 — simulate. Everything ChronoSim can express: a fire! that draws from its generator, immediate events, delayed enabling times, any initial-law rung, arbitrary stop closures. Never restricted. This tier is the product; the others are bonuses.\nTier 1 — replay and score. Requires the trajectory to determine the state sequence: no firing may draw randomness (the framework knows this from its counting-RNG detection rather than asking the author to promise), and the initial law must be foldable and scoreable — point-mass for the derived contract, with a θ-dependent law needing a log-density for the score's initial term. The trajectory likelihood, score_estimate, and gradient_record live here.\nTier 2 — pathwise/IPA and the score/IPA pairing. Additionally requires dual-safe clock distributions (Exponential, Weibull, LogNormal today — the ClockGradients.DUAL_SAFE_DISTRIBUTIONS tuple is the one source of truth); a Gamma clock refuses by name.\nTier 3 — branching and SPA. Requires the clonable world, which ChronoSim's SimulationFSM delivers (clone plus rekey_streams!, the M6 guarantee). This is a framework property, independent of the record-replay requirements, so it holds even for a fire-random model.\n\ncapability_report diagnoses the ladder for one model value, in the same report-not-throw style as check_branchable:\n\nreport = capability_report(model, θ)   # (tier0_simulate = true,\n                                       #  tier1_replay_score = false, ...,\n                                       #  unexercised = DataType[],\n                                       #  diagnostics = [\"Break's firing consumed randomness (...)\"])\n\nThe initial-law and memory-policy checks are static reads of the model value, but fire-randomness is a runtime property of fire! bodies on reachable states, so the report probes: it runs a few short seeded simulations (probe_horizon, probe_seeds keywords), reads each record's fire_random flag, and folds each record's key sequence through the derived draw-refusing fire, exercising every event family's fire! and clock distribution once on the first probe state that enables it. Each diagnostic names the responsible event family or model slot and one action that lifts the restriction — \"record its draws or model the outcome as competing events\", \"supply one, or express the initialization as time-zero events\" — never a bare MethodError.\n\nOne honesty caveat: a family the probe never enables is not silently passed. It is listed in the report's unexercised field with its own diagnostic, and the tier booleans mean \"no obstruction detected on the states the probe reached\". A caller who needs the strong reading asserts isempty(report.unexercised) and lengthens the probe until it holds.","title":"Capability tiers"},{"category":"section","location":"choosing/#Choosing-an-estimator","page":"Choosing an estimator","text":"This page is the decision guide. It names what each estimator requires, where each is valid, and how the pairing of the score estimator with the pathwise (infinitesimal-perturbation-analysis, IPA) estimator turns their disagreement into a usable verdict. Every number quoted here was measured by this package's own test suite against an exact oracle — a closed form, adaptive quadrature, or the Kolmogorov forward equations of a continuous-time Markov chain (CTMC) — and the worked example reproduces the workflow end to end.\n\nThroughout, a record is a GradientRecord: one simulated trajectory flattened to its firing sequence plus the retained survival uniform behind each firing (see Records and ingestion). All three estimators target the same quantity, the derivative ∂θ E[f(X_θ)] of an expected path functional with respect to the parameter vector θ.","title":"Choosing an estimator"},{"category":"section","location":"choosing/#The-score-function-estimator:-unbiased-everywhere,-higher-variance","page":"Choosing an estimator","text":"The score function of a trajectory is the derivative of its log-likelihood, ∂θ log L(X; θ). The score-function (likelihood-ratio) identity\n\n∂θ E[f(X)] = E[f(X) ⋅ ∂θ log L(X; θ)]\n\nholds for any path functional f, because the trajectory is held fixed and only the probability assigned to it moves with θ. score_estimate computes ∂θ log L by forward-mode automatic differentiation through a pure replay of the recorded firing sequence — the sampler never participates — and combines it with the functional value read at the recorded times, using the in-sample mean of f as a control variate.\n\nNeeds: records only. Any functional, any coupling, any distribution family with a differentiable log-density.\n\nCosts: variance that grows with path length. On the machine-repair integrated-downtime functional below, the score's standard error is roughly four times IPA's on the same 20,000 records.\n\nBuilt-in alarm: the raw score has exact mean zero (E[∂θ log L] = 0), so score_estimate reports the mean score per component. A mean score significantly different from zero means the replay's bookkeeping disagrees with the simulator that produced the records — a drift alarm that catches model/record mismatches before they corrupt an estimate.","title":"The score-function estimator: unbiased everywhere, higher variance"},{"category":"section","location":"choosing/#The-pathwise-(IPA)-estimator:-low-variance,-conditionally-valid","page":"Choosing an estimator","text":"The pathwise estimator — infinitesimal perturbation analysis (IPA) — differentiates the trajectory itself rather than its likelihood:\n\n∂θ E[f(X_θ)] = E[∂θ f(X_θ)],\n\nvalid when the path functional, with the event order and random draws frozen, is almost-surely continuous in θ. ipa_gradient holds each firing's retained uniform fixed and re-derives every firing time as a smooth function of θ through the inversion sampler's quantile; the functional read on those dual-valued times carries ∂θ from times to output.\n\nHow the retained draw is held fixed is called the coupling, and the coupling decides IPA's validity — this is the package's central measured finding. The record's coupling label is either :carry (the retained uniform is the clock's enabling draw, and a mid-flight distribution change maps the firing age through conditional survival) or :redraw (the retained uniform is the clock's last conditional draw). Records and ingestion defines both precisely.\n\nThree measured regimes:\n\nIntegrated occupancy is exact under the carry coupling. The GradientRecord built from a CompetingClocks.TrajectoryRecorder stores the total-lifetime uniform anchored at enabling — the carry coupling — and on the machine-repair model's expected integrated downtime (n = 5, λ = 0.5, μ = 1.5, horizon T = 8) IPA off those records estimated 27.13 ± 0.075 against the exact CTMC oracle 27.2216, while the score on the same records gave 27.30 ± 0.31 — agreement at a pooled z = 0.55, with IPA roughly four times tighter. On the state-dependent load-repair model the same holds in both components (carry IPA within z = [-0.35, +0.26] of the CTMC gradient [10.58, -4.93] at 8,000 replications) while the redraw coupling of the same trajectories is 38–49% biased toward zero and the pairing flags it at z = [18.9, -23.4].\nFrozen-record discrete functionals are identically zero. A functional that reads only the discrete state — who won a race, how many failures occurred — is a frozen constant of the record, so IPA reports exactly zero on every path: zero variance, one hundred percent bias. Measured: the race win-probability derivative is truly 2/9 and the terminal failure-count derivative is truly 10.727, and IPA is pinned to 0.0 on both while the score recovers each oracle.\nContended hitting times get the wrong sign. When the functional is a first-passage time whose hitting event is contested, the derivative is carried by event order, which no coupling of the times can express. On the load-repair model's first passage to three machines down, the true repair-rate derivative is +0.583 (faster repair delays the hit), while frozen-order IPA reports -0.220 — a sign flip, flagged by the pairing at z = 27.5.\n\nNeeds: records whose coupling label matches how they will be replayed, an inversion (quantile) sampling rule, and dual-safe distribution families (Exponential, Weibull, LogNormal; a Gamma clock throws a named error — see Validity and invariants).\n\nCosts: validity is a property of the (functional, coupling) pair, and an invalid IPA estimate looks confident — a wrong number with a small standard error. Never report an unpaired IPA number on a functional class you have not verified.","title":"The pathwise (IPA) estimator: low variance, conditionally valid"},{"category":"section","location":"choosing/#The-pairing:-the-default-validation-mode","page":"Choosing an estimator","text":"Run both estimators on the same records with paired_estimate. Because the score is unbiased for every functional and IPA converges to the frozen-order part, the difference of the two estimates is a consistent estimate of IPA's event-order bias. PairedGradient reports, per component, both estimates, their difference, the pooled standard error, the z-score of the difference, and a bias_detected flag at the four-standard-error threshold. The pooled standard error over-states the difference's true uncertainty (the two estimates share their records and are positively correlated), so the test is conservative: a flag means the bias is real.\n\nRead the verdict as follows. Agreement is a certificate: the cheap, low-variance IPA number can be reported. A flag is a measurement of the bias: report the score estimate instead, or reach for branching.\n\nusing ClockGradients\nusing CompetingClocks: FirstReactionMethod\nusing Distributions\nusing Random: Xoshiro\n\nimport ClockGradients: initial_state, clockkeytype, enabled, clock_distribution, fire\n\n# A two-clock exponential race, θ = [λa, λb]. Firing absorbs: `enabled` is\n# empty afterwards, so each trajectory is a single firing.\nstruct ExpRace end\ninitial_state(::ExpRace) = :racing\nclockkeytype(::ExpRace) = Symbol\nenabled(::ExpRace, s::Symbol) = s === :racing ? Symbol[:a, :b] : Symbol[]\nclock_distribution(::ExpRace, θ, key::Symbol) =\n    key === :a ? Exponential(one(eltype(θ)) / θ[1]) : Exponential(one(eltype(θ)) / θ[2])\nfire(::ExpRace, s::Symbol, key::Symbol) = key\n\nθ = [1.0, 2.0]\n\n# Functional 1: the time of the race's resolution, min(Ta, Tb) — continuous in\n# the firing times, so IPA is valid. True dE[min]/dλa = -1/(λa+λb)^2 = -1/9.\nmintime = FirstPassageTime(s -> s !== :racing)\nverdict_ok = paired_simulate_and_estimate(Xoshiro(101), ExpRace(), θ,\n    FirstReactionMethod(), mintime; nreps=8_000, horizon=100.0)\n\n# Functional 2: the indicator that clock a won — a function of the frozen\n# discrete state, so IPA is identically zero while the truth is\n# dP(a wins)/dλa = λb/(λa+λb)^2 = 2/9.\nawins = TerminalObservable(s -> s === :a ? 1.0 : 0.0)\nverdict_bias = paired_simulate_and_estimate(Xoshiro(43), ExpRace(), θ,\n    FirstReactionMethod(), awins; nreps=8_000, horizon=100.0)\n\nThe first verdict shows both estimators near -1/9 ≈ -0.111 in the first component with no flag; the second shows IPA pinned at exactly zero, the score near 2/9 ≈ 0.222, and the λa component flagged.","title":"The pairing: the default validation mode"},{"category":"section","location":"choosing/#The-branching-estimator:-event-order-sensitivity,-at-a-price","page":"Choosing an estimator","text":"The weak-derivative branching estimator (branching_gradient) recovers what IPA drops. Each inter-event step of a generalized semi-Markov process factors into a sojourn law (when the next event fires) and a selection law (which clock wins). The sojourn part differentiates smoothly as a score term. The selection part is discrete, and the estimator handles it by the Hahn–Jordan decomposition: it splits the derivative of the who-fires-next probability mass function into a positive and a negative probability vector, clones the entire running simulation twice, forces a winner drawn from each vector, continues both clones to the horizon under common random numbers, and differences the terminal functionals.\n\nNeeds: a live branchable world (not a record), because forcing a firing changes which clocks are subsequently enabled — only the running world can continue the counterfactual. The estimator is written against the nine-verb branchable-world interface: ChronoSim's SimulationFSM conforms through the ClockGradients–ChronoSim package extension, and ANY framework that implements the nine verbs for its world type — certified by check_branchable — gets the estimator unchanged. The package's own proof of that claim is a test world built directly on the raw CompetingClocks sampler layer, with no ChronoSim anywhere in it.\n\nCosts: on the machine-repair model at 800 replications the estimator spawned roughly 76 clones per replication — 38 coupled forced pairs — each continued to the horizon. A max_branches_per_rep knob truncates the branching at a documented cost: the selection term becomes biased, and the estimator warns.\n\nMeasured: on the machine-repair terminal failure count — the functional class where IPA is identically zero — branching matched the differentiated CTMC oracle [10.727, 3.568] in both components at z = [1.01, 0.04] through the ChronoSim adapter and at z = [0.46, 0.26] through the ChronoSim-free test world, and agreed with this package's own score estimator on the same model at pooled z = [1.24, 0.22].","title":"The branching estimator: event-order sensitivity, at a price"},{"category":"section","location":"choosing/#The-SPA-estimator:-the-sharper-event-order-tool","page":"Choosing an estimator","text":"Smoothed perturbation analysis (spa_gradient, Fu & Hu 1997) targets the same regime as branching — derivatives carried by event ORDER — but conditions instead of splitting. Its estimate is the pathwise (IPA) term plus a boundary term: at each event, every enabled non-winner contributes its hazard at its age, times a one-sided crossing rate (how fast the order swap approaches as θ moves), times the jump the swap would cause in the functional, estimated by one coupled clone pair fired in the two orders with the holding time between them vanishing. A second boundary term at the horizon covers clocks crossing INTO a finite observation window — on a pure Poisson counting process, where no order swap is even possible, that term alone reproduces dE[N(T)]/dλ = T exactly on every path.\n\nTwo properties keep its clone budget below branching's. A criticality gate proves most swaps can't change the functional — two speculative calls of the pure fire decide whether the pair's two orders re-coalesce (for a first passage, the gate also compares the intermediate states through the predicate) — and proven-zero jumps spawn no clones. And each clone pair is weighted by a realized hazard rather than a Hahn–Jordan mass, which measured ≈5× tighter in variance×time than branching_gradient at comparable clone budgets on the machine-repair count.\n\nNeeds: everything branching needs (a live branchable world), PLUS a pure model twin — the estimator replays records and calls fire speculatively, so the model handed to it must be the exact five-function twin of the law the world simulates (for a ClockWorld they are the same object; through the ChronoSim extension the twin is written by hand, and a per-epoch audit throws if its enabled set ever disagrees with the live world's). States must compare by value (==), clock families must be dual-safe, and a model whose enabled clocks are re-evaluated mid-flight is refused with a named error — that regime's boundary weight is not yet derived.\n\nMeasured: machine-repair terminal failure count z = [−0.55, −0.26] against the CTMC oracle where IPA is pinned zero; first passage to a contended threshold z = [−0.75, −0.25] where IPA's repair component has the WRONG SIGN (−0.24 against a true +0.93); the ≈5× variance×time advantage over branching above. The single-pair TruncatedHazard() strategy (requires the optional branch_schedule verb) uses ≈6× fewer clones at a wider standard error — a wall-clock tradeoff, not a dominance.\n\nOn the race from the pairing example — the functional the pairing flagged — SPA recovers the 2/9 that IPA zeroed, through the packaged ClockWorld with the model itself as its own twin:\n\nspa = spa_gradient(() -> ClockWorld(ExpRace(), θ; seed=1), ExpRace(), θ, awins;\n                   nreps=4_000, horizon=100.0, seed=42)\n(estimate = spa.estimate, stderr = spa.stderr, ipa_part = spa.ipa_part)","title":"The SPA estimator: the sharper event-order tool"},{"category":"section","location":"choosing/#The-decision,-in-short","page":"Choosing an estimator","text":"Simulate once, record, and run paired_estimate — one extra estimator call buys the bias verdict.\nNo flag: report the IPA estimate (tighter) and keep the score's mean-score drift alarm as a health check.\nFlag: report the score estimate. If the functional is order-sensitive and the score's variance is unacceptable, and the model exists as a live branchable world — a ChronoSim simulation via the extension, or any conforming framework — use spa_gradient when its requirements hold (pure twin, value-== states, no mid-flight re-evaluation), and branching_gradient otherwise: branching needs no replay, so it also covers clock families and couplings the pathwise machinery cannot.\nConsult the validity table before trusting any unpaired pathwise number.","title":"The decision, in short"},{"category":"section","location":"records/#Records-and-ingestion","page":"Records and ingestion","text":"Every estimator in this package except branching replays a GradientRecord: one trajectory flattened to typed arrays. This page explains what the record holds, the two ways to build one, the coupling label that governs how it may be replayed, and the segment chains that make records of state-dependent models replayable.","title":"Records and ingestion"},{"category":"section","location":"records/#What-a-record-holds","page":"Records and ingestion","text":"A GradientRecord{K} (with K the model's clock-key type) stores, per firing:\n\nkey and time — which clock fired, and when;\nlogu — the retained draw: the log of the survival-space uniform behind the firing. This is the one field that cannot be derived from the trajectory's causal structure; everything else can;\nenable_step and draw_step — back-references: the index of the earlier firing that enabled this clock, and the index of the step that supplied its last draw (0 means \"at time zero\");\nseg_offset/seg_step — a compressed-sparse-row (CSR) chain of the steps at which the firing clock's distribution was re-evaluated while it stayed enabled (one segment per firing for state-independent models);\n\nplus the observation horizon and the coupling label described below.\n\nThe record is a sufficient statistic for the trajectory: the retained-draw identity (a contract obligation on every CompetingClocks firing record — see the CompetingClocks manual's \"Contract and Invariants\" page)\n\ntime[k] == te + invlogccdf(distribution, logu[k])\n\nlets the replay reconstruct every firing time from the uniforms, and the model's own rules reconstruct everything else. The back-references are derived, never stored: enabling is a pure function of the discrete state, so a bookkeeper walks the fired keys with the model's enabled and fire rules and recovers enabling times and segment chains from the key sequence alone.","title":"What a record holds"},{"category":"section","location":"records/#Ingesting-a-TrajectoryRecorder","page":"Records and ingestion","text":"The primary source is a CompetingClocks.TrajectoryRecorder (the CompetingClocks manual's \"Recording Trajectories\" page describes it). The driver run_recorded runs a model through the real sampler with a recorder attached; ingestion then uses the recorder's stored logu verbatim and re-derives the structure:\n\nusing ClockGradients\nusing CompetingClocks: FirstReactionMethod, recorded_firings\nusing Distributions\nusing Random: Xoshiro\n\nimport ClockGradients: initial_state, clockkeytype, enabled, clock_distribution, fire\n\n# Three machines fail at rate θ[1]; a single repairman serves the head of a\n# first-in-first-out queue at rate θ[2].\nstruct Repair3 end\nstruct Repair3State\n    up::Vector{Bool}\n    queue::Vector{Int}\nend\ninitial_state(::Repair3) = Repair3State([true, true, true], Int[])\nclockkeytype(::Repair3) = Tuple{Symbol,Int}\nfunction enabled(::Repair3, s::Repair3State)\n    ks = Tuple{Symbol,Int}[]\n    for i in 1:3\n        s.up[i] && push!(ks, (:fail, i))\n    end\n    isempty(s.queue) || push!(ks, (:repair, s.queue[1]))\n    ks\nend\nclock_distribution(::Repair3, θ, key) =\n    key[1] === :fail ? Exponential(one(eltype(θ)) / θ[1]) :\n                       Exponential(one(eltype(θ)) / θ[2])\nfunction fire(::Repair3, s::Repair3State, key)\n    up, queue = copy(s.up), copy(s.queue)\n    kind, i = key\n    kind === :fail ? (up[i] = false; push!(queue, i)) :\n                     (popfirst!(queue); up[i] = true)\n    Repair3State(up, queue)\nend\n\nθ0 = [0.6, 1.4]\nrec = run_recorded(Xoshiro(20260710), Repair3(), θ0, FirstReactionMethod();\n                   horizon=10.0)\ngrec = GradientRecord(Repair3(), rec; coupling=:carry)\n(nfirings = length(grec), first_keys = grec.key[1:4], coupling = grec.coupling)","title":"Ingesting a TrajectoryRecorder"},{"category":"section","location":"records/#The-two-sided-enabling-time-audit","page":"Records and ingestion","text":"Ingestion never trusts the model. The recorder stamped an enabling time te at every enable! call, and the bookkeeper independently reconstructs te for every firing from the model's enabled rule and the fired keys. Construction asserts the two agree exactly — both are the same context time of the same enabling event, so any discrepancy means the model handed to ingestion is not the model that drove the sampler:\n\nreconstructed = reconstructed_enabling_times(Repair3(),\n    [fr.clock for fr in recorded_firings(rec)],\n    [fr.when for fr in recorded_firings(rec)])\nreconstructed == [fr.te for fr in recorded_firings(rec)]\n\nA model whose enabled rule disagrees — here, one that never cancels a failure clock while the machine is down — is rejected with a descriptive error instead of producing a silently wrong record:\n\nstruct WrongRepair3 end\ninitial_state(::WrongRepair3) = initial_state(Repair3())\nclockkeytype(::WrongRepair3) = clockkeytype(Repair3())\nclock_distribution(::WrongRepair3, θ, key) = clock_distribution(Repair3(), θ, key)\nfire(::WrongRepair3, s, key) = fire(Repair3(), s, key)\nfunction enabled(::WrongRepair3, s::Repair3State)\n    ks = Tuple{Symbol,Int}[(:fail, i) for i in 1:3]   # never cancels a fail clock\n    for i in s.queue\n        push!(ks, (:repair, i))\n    end\n    ks\nend\n\ntry\n    GradientRecord(WrongRepair3(), rec; coupling=:carry)\ncatch err\n    println(first(sprint(showerror, err), 300), \" …\")\nend","title":"The two-sided enabling-time audit"},{"category":"section","location":"records/#Ingesting-a-bare-trace","page":"Records and ingestion","text":"A record can also be built from nothing but a (key, time) firing sequence — a trace produced by any simulator, with no recorder attached — because the retained uniform is recoverable at the sampling parameter θ0. The constructor GradientRecord(model, θ0, keys, times, horizon; coupling) walks the trace with the model, folds the discrete state, detects mid-flight re-evaluations by comparing clock_distribution values at θ0 across steps, and inverts the retained-draw identity to recover each firing's uniform. The recovery is uniform across sources: on the same trajectories, the derived logu matches the recorder's stored logu to below 1e-12 (the logccdf ∘ invlogccdf round-trip):\n\nfirings = recorded_firings(rec)\nbare = GradientRecord(Repair3(), θ0,\n    [fr.clock for fr in firings], [fr.when for fr in firings], 10.0;\n    coupling=:carry)\n(structure_identical = bare.enable_step == grec.enable_step,\n max_logu_gap = maximum(abs.(bare.logu .- grec.logu)))\n\nUnlike recorder ingestion, the bare-trace constructor requires θ0: a trace carries no distributions, and both the uniform recovery and the detection of re-evaluation points need the model's clock_distribution at the parameter that generated the data.","title":"Ingesting a bare trace"},{"category":"section","location":"records/#The-coupling-label,-and-why-it-has-teeth","page":"Records and ingestion","text":"The record's coupling field names which uniform was retained, which is the same thing as naming the counterfactual the pathwise replay computes when θ moves:\n\n:carry — logu is the clock's enabling draw, the survival uniform of its total lifetime anchored at the enabling. Replay pushes that draw forward through each re-evaluation by matching conditional survival, so the clock's age is preserved across distribution changes.\n:redraw — logu is the clock's last-segment conditional draw, anchored at the last re-evaluation. Replay uses the general last-draw recurrence.\n\nBoth reproduce the recorded firing times exactly when replayed at θ0; they differ as couplings — joint constructions of the perturbed and unperturbed trajectories — once θ moves, and the difference is statistically decisive (the redraw coupling is 38–49% biased toward zero on the load-repair occupancy derivative where carry is exact; see Choosing an estimator). Because the stored uniform means a different thing under each label, replay_times dispatches on the label and each replay refuses a record of the wrong one rather than misreading it:\n\nredraw_rec = GradientRecord(Repair3(), θ0,\n    [fr.clock for fr in firings], [fr.when for fr in firings], 10.0;\n    coupling=:redraw)\ntry\n    ClockGradients._replay_carry(Repair3(), θ0, redraw_rec)\ncatch err\n    println(first(sprint(showerror, err), 220), \" …\")\nend\n\n(The public replay_times always dispatches to the replay that matches the record's own label; the guard demonstrated above protects the internal entry points, so no call path can misread a record.) An unknown label is rejected at construction; :resume-style records (chains of enable/disable pairs with age carried across disabled gaps) are explicitly out of scope in v0.","title":"The coupling label, and why it has teeth"},{"category":"section","location":"records/#Carry-chains:-state-dependent-models","page":"Records and ingestion","text":"In a state-dependent model, a still-enabled clock's distribution is re-evaluated when the state changes — a repairman that speeds up as its queue grows, mass-action failure of a shrinking pool. The record captures each such life as a segment chain: seg_step holds the step index that opened each segment, and nothing else, because states are θ-free — the discrete state after step k is pure bookkeeping produced by folding the model's fire over the recorded keys, with no parameter in it. At replay, each segment's distribution is rebuilt through the four-argument seam\n\nclock_distribution(model, θ, key, state)\n\nwith state the folded state at the segment's opening. This is the frozen- state semantics of the carry coupling made concrete: the state sequence is frozen at record-build time, and only the distributions rebuilt from it carry ∂θ. A model whose rates depend on state defines the four-argument method; a state-independent model defines only the three-argument clock_distribution(model, θ, key) and inherits the four-argument form through a dispatch fallback, so both kinds of model flow through one replay code path and existing state-independent records are the degenerate one-segment case.\n\nThe carry replay's per-segment map is the conditional-survival pushforward: entering a segment at age a with accumulated firing age af, the new age solves S_new(af') / S_new(a) = S_old(af) / S_old(a) in survival functions S. For exponential segments this reduces to the Gibson–Bruck rescaling (remaining time scales by the rate ratio), which pins the implementation:\n\n# One clock :x enabled at t = 0 with rate θ[1]; a forcing event at τ flips the\n# state so :x is re-evaluated to rate θ[2]. θ = [r1, r2, rforce].\nstruct TwoSegment end\ninitial_state(::TwoSegment) = :s1\nclockkeytype(::TwoSegment) = Symbol\nenabled(::TwoSegment, s::Symbol) =\n    s === :s1 ? Symbol[:x, :force] : s === :s2 ? Symbol[:x] : Symbol[]\nfunction clock_distribution(::TwoSegment, θ::AbstractVector, key::Symbol, s::Symbol)\n    key === :force && return Exponential(one(eltype(θ)) / θ[3])\n    s === :s1 ? Exponential(one(eltype(θ)) / θ[1]) : Exponential(one(eltype(θ)) / θ[2])\nend\nfire(::TwoSegment, s::Symbol, key::Symbol) = key === :force ? :s2 : :done\n\nr1, r2, τ, u = 0.8, 1.9, 1.3, 0.3\nθseg = [r1, r2, 1.0]\nsched = -log(u) / r1                        # :x's scheduled time from its enabling draw\ntfire = τ + (r1 / r2) * (sched - τ)         # the Gibson–Bruck rescaling, by hand\n\nchainrec = GradientRecord(TwoSegment(), θseg, [:force, :x], [τ, tfire], 100.0;\n                          coupling=:carry)\n(nsegments = ClockGradients.nsegments(chainrec, 2),\n recovered_u = exp(chainrec.logu[2]),          # the enabling draw, recovered from the trace\n replay_matches = replay_times(TwoSegment(), θseg, chainrec)[2] ≈ tfire)\n\nThe bookkeeper found the two-segment chain from the trace alone, recovered the enabling uniform u = 0.3 by inverting the chain backward, and the carry replay independently reproduced the hand-computed firing time.","title":"Carry chains: state-dependent models"},{"category":"section","location":"records/#Where-the-enabling-times-go-during-a-pathwise-replay","page":"Records and ingestion","text":"One design point matters to anyone extending the package: during replay_times at a dual-valued θ, a clock's enabling time is an earlier replayed firing time, so enabling times are dual-valued too. The model-contract helper sync_enabling_times! is deliberately generic in its value type for exactly this reason — the enabling-time table is the channel through which ∂θ propagates down the firing sequence.","title":"Where the enabling times go during a pathwise replay"},{"category":"section","location":"reference/#API-reference","page":"API reference","text":"Docstrings for every exported name, grouped as the source is grouped. The package module's own docstring states the estimator identities.","title":"API reference"},{"category":"section","location":"reference/#Hazard-helpers","page":"API reference","text":"Our own generic functions over Distributions.jl types — the function names belong to this module, so defining methods on UnivariateDistribution is augmentation, not piracy.","title":"Hazard helpers"},{"category":"section","location":"reference/#The-model-contract","page":"API reference","text":"A model is five extendable generic functions plus one shared bookkeeping helper; θ enters through clock_distribution only.","title":"The model contract"},{"category":"section","location":"reference/#Records","page":"API reference","text":"The framework-record ingestion seam. The core defines no methods; a simulation framework's package extension attaches one method per record type it can ingest (the ClockGradients–ChronoSim extension attaches the ChronoSim.MinimalRecord method).","title":"Records"},{"category":"section","location":"reference/#Path-functionals","page":"API reference","text":"","title":"Path functionals"},{"category":"section","location":"reference/#The-score-function-estimator","page":"API reference","text":"","title":"The score-function estimator"},{"category":"section","location":"reference/#The-pathwise-(IPA)-estimator","page":"API reference","text":"","title":"The pathwise (IPA) estimator"},{"category":"section","location":"reference/#The-score/IPA-pairing","page":"API reference","text":"","title":"The score/IPA pairing"},{"category":"section","location":"reference/#The-branchable-world-protocol","page":"API reference","text":"The nine duck-typed verbs a framework implements for its world type to receive the branching estimator (see The branchable-world interface), and the conformance harness that certifies the semantic obligations. ChronoSim's SimulationFSM conforms through the ClockGradients–ChronoSim package extension.\n\nThe optional tenth verb, required only by the SPA estimator's truncated-hazard weight strategy:","title":"The branchable-world protocol"},{"category":"section","location":"reference/#The-packaged-world","page":"API reference","text":"A minimal simulation runner implementing every verb, so a pure five-function model runs the clone-based estimators without a simulation framework.","title":"The packaged world"},{"category":"section","location":"reference/#The-branching-estimator","page":"API reference","text":"The estimator lives in the core package, written only against the protocol above; the ChronoSim package extension adds the convenience method that takes a (sim_factory, initializer) pair.","title":"The branching estimator"},{"category":"section","location":"reference/#The-SPA-estimator","page":"API reference","text":"Smoothed perturbation analysis: the IPA term plus hazard-weighted boundary terms at event-order swaps and at the horizon, with jumps from coupled clone pairs. The extension adds the convenience method taking a (sim_factory, initializer, model, θ, fn) tuple, where model is the pure twin of the simulated law.","title":"The SPA estimator"},{"category":"module","location":"reference/#ClockGradients.ClockGradients","page":"API reference","text":"ClockGradients\n\nThe derivative-estimator layer above CompetingClocks.jl. It consumes the sampler package's trajectory records (the retained-draw identity is the seam) and turns them into Monte Carlo estimates of ∂θ E[f(X_θ)] for a generalized-semi-Markov process (GSMP) model.\n\nThe package holds three estimator families over one shared record and model contract. The score-function (likelihood-ratio) estimator uses\n\n∂θ E[f(X)] = E[f(X) ⋅ ∂θ log L(X; θ)],\n\nwith the trajectory held fixed and θ entering only through the model's clock_distribution; forward-mode automatic differentiation (ForwardDiff) carries ∂θ log L through a pure replay of the recorded firing sequence — the sampler never participates in the differentiation, it only produced the record. The pathwise/IPA estimator instead freezes the retained uniforms and the event order and differentiates the replayed firing times themselves (replay_times, ipa_gradient), including through mid-flight re-evaluation chains under the :carry/:redraw coupling labels. paired_estimate runs both on the same records and turns their disagreement into a bias verdict. The weak-derivative branching estimator (branching_gradient) recovers event-order sensitivity by cloning a live simulation; it is written against the nine-verb branchable-world protocol (branch_peek, branch_commit!, ...), so any framework whose world implements the protocol — certified by check_branchable — gets it, and the sibling event-driven framework adopts it through a package extension.\n\nDesign findings this package exists to extract are recorded in the WorldTimer knowledge/ notes; the code favors concrete clock-key types K, flat typed record arrays, and θ isolated behind clock_distribution so the replay loops stay type-stable under a dual-valued θ.\n\n\n\n\n\n","title":"ClockGradients.ClockGradients"},{"category":"function","location":"reference/#ClockGradients.loghazard","page":"API reference","text":"loghazard(d::UnivariateDistribution, t::Real) -> Real\n\nLog hazard rate log h(t) = log f(t) − log S(t) of lifetime distribution d at age t, computed as logpdf(d, t) − logccdf(d, t). This is the log-density increment a firing contributes to a path log-likelihood: over an interval a clock contributes its conditional log-survival, and at its own firing it adds this log-hazard, so the two together are the conditional log firing density.\n\n\n\n\n\n","title":"ClockGradients.loghazard"},{"category":"function","location":"reference/#ClockGradients.hazard","page":"API reference","text":"hazard(d::UnivariateDistribution, t::Real) -> Real\n\nHazard rate h(t) = f(t) / S(t) of d at age t, exp(loghazard(d, t)). Kept in the log-domain internally so a deep survival tail does not overflow the ratio.\n\n\n\n\n\n","title":"ClockGradients.hazard"},{"category":"function","location":"reference/#ClockGradients.conditional_remaining","page":"API reference","text":"conditional_remaining(d, age, u) -> Real\n\nRemaining time until a clock with lifetime distribution d fires, given it has already survived to age, driven by the survival-space uniform variate u:\n\nconditional_remaining(d, age, u) = invlogccdf(d, log(u) + logccdf(d, age)) − age.\n\nThis is the inversion sampling rule, and its inversion form is load-bearing for the pathwise/IPA estimator that consumes these records (CG-M2). The inversion-sampler contract (from the PathwiseIPA prototype) is:\n\nFor a fixed uniform u, the drawn value is a SMOOTH function of the distribution's parameters. Replaying a retained u through a dual-valued d therefore propagates ∂θ into the firing time — that smoothness is the property IPA differentiates. A rejection sampler has no such property: its accept/reject decision can flip under an infinitesimal parameter perturbation, so IPA-by-retained-draws REQUIRES an inversion sampler.\n\nThe score estimator (CG-M1) does not itself need this smoothness — it differentiates the likelihood, not the draw — but sharing one sampling rule across estimators keeps the trajectories comparable, and CompetingClocks' TrajectoryRecorder stores exactly the logu = logccdf(d, when − te) this rule inverts.\n\n\n\n\n\n","title":"ClockGradients.conditional_remaining"},{"category":"function","location":"reference/#ClockGradients.initial_state","page":"API reference","text":"initial_state(model)\n\nThe trajectory's state at time zero. Treated as IMMUTABLE by every consumer: fire must return a fresh state rather than mutate its argument, because the likelihood replay and the functional fold revisit earlier states and any aliasing between steps would corrupt the enabled-set bookkeeping.\n\n\n\n\n\n","title":"ClockGradients.initial_state"},{"category":"function","location":"reference/#ClockGradients.clockkeytype","page":"API reference","text":"clockkeytype(model) -> Type\n\nThe concrete type K of this model's clock keys. Returned as a type (not inferred from a sample) so records, dictionaries, and replay loops can be built with a concrete K up front — the type-stability lever for the whole package. The CompetingClocks sampler is built with this same K.\n\n\n\n\n\n","title":"ClockGradients.clockkeytype"},{"category":"function","location":"reference/#ClockGradients.enabled","page":"API reference","text":"enabled(model, state) -> iterable of clock keys\n\nThe clocks active in state, each identified by a key of type clockkeytype(model).\n\nDETERMINISTIC-ORDER REQUIREMENT: the returned iterable must list the enabled keys in an order that depends only on state, not on hash iteration order or allocation history. The record builder, the driver, and the replay all walk this iterable, and while the score is a sum (order-insensitive), the pathwise replay's buffer indices and any RNG-consuming driver ARE order-sensitive; a model that returns keys in a nondeterministic order silently desynchronizes the sampler from the replay. Return a Vector built by a fixed loop, not the keys of a Dict or the elements of a Set.\n\n\n\n\n\n","title":"ClockGradients.enabled"},{"category":"function","location":"reference/#ClockGradients.clock_distribution","page":"API reference","text":"clock_distribution(model, θ::AbstractVector, key) -> UnivariateDistribution\n\nThe lifetime distribution of clock key, measured from the clock's enabling time, at parameter vector θ. This is the SOLE seam through which θ enters the framework.\n\nTwo idioms make this function safe under a dual-valued θ (the case that makes ForwardDiff produce a gradient):\n\nDUAL-θ REBUILD. Because θ is only ever read here, the estimators call this function again with a ForwardDiff.Dual-valued θ to differentiate; the method must therefore build its distribution arithmetically from θ and return whatever element type that arithmetic produces — never capture a primal Float64 rate from an outer scope.\nELTYPE-STABLE CONSTRUCTION. Write rate/scale expressions so that every branch returns the SAME concrete distribution type under a given eltype(θ). The idiom is one(eltype(θ)) / θ[i] (equivalently inv(θ[i])): a bare 1 / θ[i] also promotes, but writing one(eltype(θ)) documents that the numerator's job is to carry the element type. For example a rate-θ[1] exponential clock is Exponential(one(eltype(θ)) / θ[1]), which is Exponential{Float64} at a primal θ and Exponential{Dual} at a dual θ — one concrete type per call, so the replay loop that calls it stays type-stable.\n\n\n\n\n\n","title":"ClockGradients.clock_distribution"},{"category":"function","location":"reference/#ClockGradients.fire","page":"API reference","text":"fire(model, state, key) -> new state\n\nThe (pure, deterministic) state transition when clock key fires. Returns a NEW state; must not mutate state (see initial_state). θ does not appear: firing changes the discrete state, and only which distributions are then enabled — never a rate — depends on θ.\n\n\n\n\n\n","title":"ClockGradients.fire"},{"category":"function","location":"reference/#ClockGradients.sync_enabling_times!","page":"API reference","text":"sync_enabling_times!(te::AbstractDict{K,V}, enabled_keys, now) -> te\n\nApply the GSMP clock-retention rule to the enabling-time table te in place: a key already present keeps its stored enabling time as long as it is still in enabled_keys; a key that has left the enabled set is deleted (its clock is cancelled); a newly enabled key is inserted with enabling time now.\n\nGSMP SEMANTICS. A clock keeps its enabling time — and therefore its age — as long as its key stays continuously in the enabled set across transitions. Leaving the set cancels the clock; the fired clock is always removed by the caller before the next sync, so a key re-enabled immediately after firing starts a FRESH clock at the current time. This is the identical rule the driver applies through enable!/disable! and the record builder's Bookkeeper applies offline, which is why the enabling times the sampler stamps match the ones the replay reconstructs.\n\nThe value type V is GENERIC on purpose. In the score replay V is Float64 (the recorded firing times are constants). In the pathwise/IPA replay (CG-M2) V is a ForwardDiff.Dual: an enabling time there IS an earlier replayed firing time, so keeping V open makes this same table the channel through which ∂θ propagates down the firing sequence.\n\n\n\n\n\n","title":"ClockGradients.sync_enabling_times!"},{"category":"type","location":"reference/#ClockGradients.GradientRecord","page":"API reference","text":"GradientRecord{K}\n\nOne trajectory flattened to typed arrays for offline replay. The per-firing arrays (key, time, logu, enable_step, draw_step) are length n (the number of recorded firings); the segment arrays (seg_offset, seg_step) hold the CSR chain of distribution re-evaluations.\n\nFields\n\nkey::Vector{K} — the clock that fired at each step.\ntime::Vector{Float64} — the absolute firing time at each step.\nlogu::Vector{Float64} — the retained survival log-uniform. Its MEANING depends on coupling: under :carry it is the clock's ENABLING-draw uniform (logccdf(d₁, af₁) on the enabling-segment distribution), and under :redraw it is the LAST-segment conditional uniform (identity (R) anchored at draw_step). Both reproduce the recorded firing time on replay at θ0; they differ as pathwise couplings once θ moves. This is the one field not derivable from the causal structure.\nenable_step::Vector{Int} — for firing k, the step that enabled its clock; 0 means enabled at t = 0, so te_k = enable_step[k] == 0 ? 0 : time[enable_step[k]].\ndraw_step::Vector{Int} — the step that supplied the clock's last draw (the last distribution change while enabled, or the enabling if none). Equals enable_step when the clock was never re-evaluated; ≠ enable_step is the mid-flight-re-evaluation case CG-M3 makes real.\nseg_offset::Vector{Int} — length n + 1; the segments of firing k are the flat indices seg_offset[k] : seg_offset[k+1]-1.\nseg_step::Vector{Int} — flat CSR array: for each firing, the step index that OPENED each of its segments (0 = enabled at time zero, then each re-evaluation step). A segment's distribution is rebuilt at replay by clock_distribution(model, θ, key, state) with state the folded discrete state after seg_step — never stored, because states are θ-free. The plain (state-independent) case is the degenerate one-segment-per-firing chain, so the replay dispatch is single.\nhorizon::Float64 — the observation horizon; survival (censoring) terms of clocks still enabled at the end extend to here. May be Inf.\ncoupling::Symbol — the re-evaluation coupling label (:redraw or :carry). It now has TEETH: replay_times dispatches on it and the carry/redraw replays refuse a record of the wrong label (a :carry record's enabling uniform replayed through the redraw recurrence, or vice versa, is silently wrong).\n\n\n\n\n\n","title":"ClockGradients.GradientRecord"},{"category":"type","location":"reference/#ClockGradients.Bookkeeper","page":"API reference","text":"Bookkeeper(model)             # θ0-less: state-INDEPENDENT models only\nBookkeeper(model, θ0)         # recommended: detects mid-flight re-evaluation\n\nThe framework bookkeeping of one trajectory walk, minus randomness: the current state, the current time, and — for each currently enabled clock key — its enabling time te and the per-clock SEGMENT CHAIN chain[key], a vector of step indices [enable_step, change₁, change₂, …] (0 meaning \"enabled at time zero\"). The first entry is the enabling step; each later entry is a step at which the clock's distribution was re-evaluated. enable_step is chain[key][1] and draw_step (the last re-evaluation, or the enabling if none) is chain[key][end].\n\nDetecting a re-evaluation requires evaluating the clock's distribution, so the θ0-taking constructor is the one that can find chains: after each firing it rebuilds clock_distribution(model, θ0, key, state) for every still-enabled clock and compares it with == against the previous step's value, opening a new segment on a change. The θ0-LESS constructor cannot compare distributions and therefore ASSUMES no clock is ever re-evaluated — a valid assumption ONLY for a model whose clock distributions are state-independent (every chain stays a single enabling segment). It exists for the recorder-ingestion path, whose CompetingClocks runs never re-evaluate a live clock; prefer the θ0 form otherwise.\n\nWalk it by reading winner_chain(bk, key) for the firing about to be applied and then calling advance!(bk, k, key, time).\n\n\n\n\n\n","title":"ClockGradients.Bookkeeper"},{"category":"function","location":"reference/#ClockGradients.reconstructed_enabling_times","page":"API reference","text":"reconstructed_enabling_times(model, keys, times) -> Vector{Float64}\n\nThe enabling time te the Bookkeeper reconstructs for the WINNER of each firing, computed purely from the model's enabled rule and the fired keys — never read from a stored record. Pinned in tests against the te the CompetingClocks recorder stamped at each enable! (the fr.te field), so the two independent bookkeepers must agree exactly: this is the two-sided audit that the recorded firing sequence is a sufficient statistic for the GSMP ages. Uses the θ0-less walk (te does not depend on θ; only segment detection does).\n\n\n\n\n\n","title":"ClockGradients.reconstructed_enabling_times"},{"category":"function","location":"reference/#ClockGradients.gradient_record","page":"API reference","text":"gradient_record(model, framework_record, θ0) -> GradientRecord\n\nThe FRAMEWORK-RECORD ingestion seam: convert a simulation framework's own trajectory record into a GradientRecord at the sampling parameter θ0, so score_estimate / ipa_estimate / paired_estimate consume the framework's records directly. The core package defines NO methods — it does not know any framework's record schema (and, by the structural pin in the tests, never names one). A framework's package extension attaches one method per record type it can ingest, typically over the bare-trace GradientRecord constructor (the record contributes the (key, time) firings, the horizon, and the coupling label; the extension refuses records whose firing sequence is not a deterministic function of the initial condition).\n\n\n\n\n\n","title":"ClockGradients.gradient_record"},{"category":"type","location":"reference/#ClockGradients.PathFunctional","page":"API reference","text":"PathFunctional\n\nAbstract supertype of a path functional. Its concrete subtype encodes the functional's smoothness class, which determines whether pathwise/IPA differentiation is valid for it (CG-M2 consumes that information).\n\n\n\n\n\n","title":"ClockGradients.PathFunctional"},{"category":"type","location":"reference/#ClockGradients.IntegratedOccupancy","page":"API reference","text":"IntegratedOccupancy(g)\n\nThe time integral ∫₀ᵀ g(x_t) dt of a state observable g. The pathwise-smooth form: the integrand is a step function of time whose only θ-dependence is the step LOCATIONS (the firing times), so its derivative is a clean sum over intervals.\n\n\n\n\n\n","title":"ClockGradients.IntegratedOccupancy"},{"category":"type","location":"reference/#ClockGradients.TerminalObservable","page":"API reference","text":"TerminalObservable(g)\n\nThe state observable at the horizon, g(x_T). The jumpy form: as θ varies the firing times shift but x_T (a discrete state) is piecewise constant, so under a fixed firing ORDER pathwise/IPA sees a FROZEN constant and reports a zero derivative — that zero is the IPA failure mode, and the score estimator is what recovers the true (nonzero) derivative for this class.\n\n\n\n\n\n","title":"ClockGradients.TerminalObservable"},{"category":"type","location":"reference/#ClockGradients.FirstPassageTime","page":"API reference","text":"FirstPassageTime(pred)\n\nThe first time the state satisfies predicate pred, inf{t : pred(x_t)}. Pathwise-smooth exactly when the hitting STEP is θ-stable (the same firing remains the one that first satisfies pred under an infinitesimal θ change).\n\n\n\n\n\n","title":"ClockGradients.FirstPassageTime"},{"category":"function","location":"reference/#ClockGradients.lower","page":"API reference","text":"lower(fn::PathFunctional, model, record::GradientRecord) -> lowered struct\nlower(fn::PathFunctional, model, keys, horizon) -> lowered struct\n\nLower a path functional against a trajectory to a flat, θ-free struct. The GradientRecord form reads the record's key sequence and horizon; the (keys, horizon) form is for hand-built trajectories in tests.\n\n\n\n\n\n","title":"ClockGradients.lower"},{"category":"function","location":"reference/#ClockGradients.evaluate","page":"API reference","text":"evaluate(lowered, times::AbstractVector) -> value\n\nEvaluate a lowered functional on a times vector, purely. For an occupancy it is the interval fold; for a terminal observable it is the frozen value plus a zero(eltype(times)) term so a dual times vector still flows an honest (zero) derivative rather than short-circuiting to a Float64; for a first passage it is times[hit_step].\n\n\n\n\n\n","title":"ClockGradients.evaluate"},{"category":"function","location":"reference/#ClockGradients.value_at_record","page":"API reference","text":"value_at_record(fn::PathFunctional, model, record::GradientRecord) -> Float64\n\nThe functional read at the RECORDED firing times — the value f(X) the score estimator averages. Lowers fn against the record and evaluates it on record.time.\n\n\n\n\n\n","title":"ClockGradients.value_at_record"},{"category":"function","location":"reference/#ClockGradients.score_loglikelihood","page":"API reference","text":"score_loglikelihood(model, θ, record::GradientRecord) -> log L\n\nLog-likelihood of the recorded trajectory as a pure function of θ, the trajectory held fixed. Walks the recorded firing sequence, reconstructing the enabled set and every clock's GSMP enabling time from the model's own rules and the fired keys alone (never reading a record back-reference), and accumulates:\n\nover each inter-event interval, every enabled clock's conditional log-survival increment logccdf(d, t1 − te) − logccdf(d, t − te);\nat each firing, the winner's loghazard(d, t1 − te), so its terms together are its conditional firing log-density;\nat the horizon, a censoring survival term for every clock still enabled.\n\nθ enters only through clock_distribution, so a dual-valued θ makes the return value carry ∂θ log L. The enabling-time table holds Float64 (recorded times are constants) while θ carries the duals through the distributions, which keeps the loop type-stable: ll has element type eltype(θ).\n\n\n\n\n\n","title":"ClockGradients.score_loglikelihood"},{"category":"function","location":"reference/#ClockGradients.score_gradient","page":"API reference","text":"score_gradient(model, θ, record::GradientRecord) -> Vector\n\n∂θ log L of one recorded trajectory, by forward-mode AD through score_loglikelihood.\n\n\n\n\n\n","title":"ClockGradients.score_gradient"},{"category":"function","location":"reference/#ClockGradients.score_estimate","page":"API reference","text":"score_estimate(model, θ, records::Vector{GradientRecord}, fn::PathFunctional)\n\nEstimate ∂θ E[f(X_θ)] from PRE-RECORDED trajectories via the score-function identity with the in-sample f̄ control variate. For each record it reads the functional value at the recorded times and the score by AD replay, then combines them. Returns a NamedTuple:\n\nestimate   — the derivative estimate, one entry per θ component;\nstderr     — its standard error, per component;\nscoremean  — mean of the raw score, per component: the E[score] = 0 DRIFT                ALARM the tests assert as |scoremean / scorestderr| < 4;\nscorestderr— standard error of the raw score, per component;\nfmean, fstderr — the functional's mean and its standard error;\nnreps.\n\n\n\n\n\n","title":"ClockGradients.score_estimate"},{"category":"function","location":"reference/#ClockGradients.run_recorded","page":"API reference","text":"run_recorded(rng, model, θ, method; horizon) -> TrajectoryRecorder\n\nDrive model at parameter θ through a CompetingClocks.SamplingContext built with sampler method (FirstReactionMethod() or NextReactionMethod()), with a TrajectoryRecorder attached, censoring at horizon. Returns the closed recorder; read its firings with recorded_firings, or pass it straight to GradientRecord(model, rec; coupling=...).\n\n\n\n\n\n","title":"ClockGradients.run_recorded"},{"category":"function","location":"reference/#ClockGradients.simulate_and_estimate","page":"API reference","text":"simulate_and_estimate(rng, model, θ, method, fn; nreps, horizon, coupling=:redraw)\n\nConvenience driver: run nreps trajectories of model at θ through the real CompetingClocks sampler (run_recorded), ingest each into a GradientRecord, and hand them to score_estimate. Returns the same NamedTuple as score_estimate.\n\nThe score numbers do not depend on coupling (the likelihood replay never reads the retained uniforms), and for the records this driver builds the label is also replay-equivalent: run_recorded never re-evaluates a live clock, so every chain is a single segment and the carry and redraw replays coincide. The label matters once the same records feed a pathwise replay of a genuinely re-evaluated model — see GradientRecord.\n\n\n\n\n\n","title":"ClockGradients.simulate_and_estimate"},{"category":"function","location":"reference/#ClockGradients.replay_times","page":"API reference","text":"replay_times(model, θ, record::GradientRecord) -> Vector{T}\n\nRe-derive every firing TIME of the recorded trajectory as a function of θ, with the retained uniforms and the event order frozen. T = typeof(one(eltype(θ)) * 1.0), so this runs at Float64 (reproducing record.time to round-off — the pinned identity) and at ForwardDiff.Dual (carrying ∂θ into the times).\n\nThe replay DISPATCHES on record.coupling — this is where the coupling label grows teeth. Both replays fold the θ-free discrete state once and rebuild each segment's distribution with the four-argument clock_distribution(model, θ, key, state), so a state-dependent model flows ∂θ through re-evaluated rates while a state-independent one inherits the three-argument form:\n\n:redraw — the general last-draw recurrence times[k] = te + invlogccdf(d_last, logu[k] + logccdf(d_last, tdraw − te)), with d_last the distribution over the clock's LAST segment (at the draw_step state) and tdraw = times[draw_step]. For a never-re-evaluated clock draw_step == enable_step, tdraw == te, the correction term is logccdf(d, 0) = 0, and this collapses to the CG-M1/M2 recurrence.\n:carry — the conditional-survival chain pushforward: start from the enabling age invlogccdf(d₁, logu[k]) and map it through each segment by matching conditional survival, a_f ← invlogccdf(d_new, logccdf(d_new, a) + logccdf(d, a_f) − logccdf(d, a)), where the segment ages a = times[seg_step] − te are themselves REPLAYED (dual) quantities. A single-segment chain reduces to te + invlogccdf(d₁, logu[k]).\n\nUnder a dual θ every segment distribution must be dual-safe (Exponential, Weibull, LogNormal); a Gamma or other Rmath-quantile family raises the documented ArgumentError.\n\n\n\n\n\n","title":"ClockGradients.replay_times"},{"category":"function","location":"reference/#ClockGradients.ipa_gradient","page":"API reference","text":"ipa_gradient(model, θ, record::GradientRecord, fn::PathFunctional) -> Vector\n\nOne trajectory's pathwise derivative ∂θ f(X_θ): lower fn once against the frozen record (a Float64 object), then forward-mode AD of θ -> evaluate(lowered, replay_times(model, θ, record)). For a TerminalObservable (or any functional that reads only the frozen discrete state) this is identically the zero vector — the IPA failure mode the pairing detects.\n\n\n\n\n\n","title":"ClockGradients.ipa_gradient"},{"category":"function","location":"reference/#ClockGradients.ipa_estimate","page":"API reference","text":"ipa_estimate(model, θ, records::Vector{GradientRecord}, fn::PathFunctional)\n\nEstimate ∂θ E[f(X_θ)] from PRE-RECORDED trajectories via the pathwise/IPA identity: the sample mean of each record's ipa_gradient. Returns a NamedTuple:\n\nestimate  — the derivative estimate, one entry per θ component;\nstderr    — its standard error, per component;\nnreps;\nper_path  — the D×nreps matrix of per-path gradients, so a caller can               assert the pinned exact zeros of a frozen functional.\n\nIPA is unbiased only when the frozen-order path functional is almost-surely continuous in θ; when it is not, estimate is a confident WRONG number with a small standard error, which is precisely why this estimator ships paired with the score estimator (paired_estimate) as a bias detector.\n\n\n\n\n\n","title":"ClockGradients.ipa_estimate"},{"category":"function","location":"reference/#ClockGradients.ipa_simulate_and_estimate","page":"API reference","text":"ipa_simulate_and_estimate(rng, model, θ, method, fn; nreps, horizon,\n                          coupling=:redraw)\n\nConvenience driver mirroring simulate_and_estimate for the score estimator: run nreps trajectories of model at θ through the real CompetingClocks sampler (run_recorded), ingest each into a GradientRecord, and hand them to ipa_estimate. Returns the same NamedTuple as ipa_estimate.\n\n\n\n\n\n","title":"ClockGradients.ipa_simulate_and_estimate"},{"category":"type","location":"reference/#ClockGradients.PairedGradient","page":"API reference","text":"PairedGradient\n\nThe verdict of running the score and IPA estimators on one shared set of records, per θ component.\n\nFields\n\nscore, score_stderr — the score-function estimate and its standard error (the unbiased reference);\nipa, ipa_stderr — the pathwise/IPA estimate and its standard error (the cheap, conditionally-valid estimate);\ndifference — score − ipa, a consistent estimate of IPA's event-order bias;\ndiff_stderr — the POOLED standard error sqrt(score_stderr² + ipa_stderr²). Both estimators run on the same records, so they are positively correlated and the true difference variance is smaller; the pooled form OVER-estimates it, which makes the bias test conservative (a flag means the bias is real);\nz — difference / diff_stderr, per component;\nbias_detected — abs(z) > 4, per component: IPA is significantly biased on this functional for that θ component;\nnreps.\n\n\n\n\n\n","title":"ClockGradients.PairedGradient"},{"category":"function","location":"reference/#ClockGradients.paired_estimate","page":"API reference","text":"paired_estimate(model, θ, records::Vector{GradientRecord}, fn) -> PairedGradient\n\nRun the score estimator and the IPA estimator on the SAME records and return the paired verdict (PairedGradient). Because the records are shared, the difference of the two estimates isolates IPA's event-order bias rather than adding two independent Monte Carlo errors.\n\n\n\n\n\n","title":"ClockGradients.paired_estimate"},{"category":"function","location":"reference/#ClockGradients.paired_simulate_and_estimate","page":"API reference","text":"paired_simulate_and_estimate(rng, model, θ, method, fn; nreps, horizon,\n                             coupling=:redraw) -> PairedGradient\n\nConvenience driver: simulate nreps trajectories once, ingest them, and run paired_estimate on that single shared record set (so score and IPA see identical paths).\n\n\n\n\n\n","title":"ClockGradients.paired_simulate_and_estimate"},{"category":"function","location":"reference/#ClockGradients.branch_peek","page":"API reference","text":"branch_peek(world) -> Union{Nothing, Tuple{Float64,K}}\n\nThe next natural firing of world as a (time, clock_key) tuple, or nothing when no further firing is scheduled (the enabled set is empty or the next time is not finite).\n\nSEMANTIC OBLIGATIONS. Peeking is NON-COMMITTING and REPEATABLE: two consecutive branch_peek calls with no intervening mutation must return equal answers and leave the world's subsequent behavior unchanged. The returned tuple is a reservation; the caller either commits it with branch_commit! or abandons it (the fixed-horizon stopping pattern). A sampler whose native next redraws on every call cannot back this verb directly — the world must cache the reservation until the next mutation invalidates it.\n\n\n\n\n\n","title":"ClockGradients.branch_peek"},{"category":"function","location":"reference/#ClockGradients.branch_commit!","page":"API reference","text":"branch_commit!(world, key, tstar)\n\nCommit the peeked firing: clock key fires at time tstar, driven through the framework's NORMAL update path — state transition, clock cancellation and creation, and time advance, exactly as an ordinary simulation step.\n\n(tstar, key) must be the tuple the current branch_peek returned. After the call, branch_time equals tstar.\n\n\n\n\n\n","title":"ClockGradients.branch_commit!"},{"category":"function","location":"reference/#ClockGradients.branch_force!","page":"API reference","text":"branch_force!(world, key, tstar)\n\nFire the CHOSEN enabled clock key at time tstar regardless of which clock would have won the race — the branch step of the weak-derivative estimator. The firing must run through the SAME update path as a natural firing (the resulting world depends on which transition ran, not on why it ran); only the treatment of the racing clocks differs, with every surviving clock left distributed by its lifetime law conditioned on survival past tstar.\n\nPRECONDITION (keep-if-later unbiasedness): tstar must be the current race's decision time — the time component of the world's own branch_peek, or of a coupled original's peek when world is a clone taken at the same instant. A backend that keeps a surviving clock's schedule when it lies beyond tstar is proven correct exactly under that choice; picking tstar by inspecting a survivor's schedule biases the kept branch.\n\n\n\n\n\n","title":"ClockGradients.branch_force!"},{"category":"function","location":"reference/#ClockGradients.branch_clone","page":"API reference","text":"branch_clone(world) -> world′\n\nA COUPLED full copy of the running world: state, clocks, ages, and the state of every random stream are copied, so that with no intervening branch_rekey! the clone's subsequent peek/commit sequence is identical to the original's — bit for bit, firing for firing. Cloning must not perturb the original (taking a clone and discarding it leaves the original's future unchanged). Divergence is an explicit act, via branch_rekey!.\n\n\n\n\n\n","title":"ClockGradients.branch_clone"},{"category":"function","location":"reference/#ClockGradients.branch_rekey!","page":"API reference","text":"branch_rekey!(world, seed)\n\nGive the world FRESH randomness derived deterministically from seed. After the call, the world's continuation from the current instant is a fresh draw from the model's law given the current state and clock ages — which requires both re-seeding the streams that future draws come from AND redrawing any already-scheduled firing time, conditioned on its clock's survival to the current time (a resample at a stopping time, so the law is unchanged). Re-seeding alone is NOT enough for a backend that caches scheduled firings: the cached times would replay the old randomness.\n\nTwo obligations, both load-bearing for the estimator:\n\nDECOUPLING: after rekeying, the world's continuation is independent of the continuation the un-rekeyed original would have produced. In particular the very next firing time is a fresh draw. branching_gradient relies on this to decouple replications that all start from one factory-built world.\nCOUPLING BY SEED: two clones of the same world rekeyed to the SAME seed produce identical continuations (common random numbers across the pair), while remaining decoupled from the original. The Hahn–Jordan branch pair is rekeyed to one shared seed so the difference of its two functionals has low variance.\n\n\n\n\n\n","title":"ClockGradients.branch_rekey!"},{"category":"function","location":"reference/#ClockGradients.branch_time","page":"API reference","text":"branch_time(world) -> Float64\n\nThe world's current simulation time: the time of the most recently committed or forced firing (or the start time before any firing).\n\n\n\n\n\n","title":"ClockGradients.branch_time"},{"category":"function","location":"reference/#ClockGradients.branch_enabled_ages","page":"API reference","text":"branch_enabled_ages(world) -> Vector{Tuple{K,Float64}}\n\nEvery currently-enabled clock paired with its age at the current time, branch_time(world) - te where te is the clock's enabling time. Ages are nonnegative, and the vector is SORTED BY KEY. The deterministic order is load-bearing: the estimator builds the who-fires-next probability vector in this order and indexes the Hahn–Jordan draws back into it, so two coupled worlds must present the same clocks in the same positions. A caller that needs ages at a later instant t (the race's decision time) shifts every age by t - branch_time(world); the enabling times, not the ages, are what the world holds fixed.\n\n\n\n\n\n","title":"ClockGradients.branch_enabled_ages"},{"category":"function","location":"reference/#ClockGradients.branch_clock_distribution","page":"API reference","text":"branch_clock_distribution(world, θ::AbstractVector, key) -> UnivariateDistribution\n\nThe lifetime distribution of the enabled clock key, measured from that clock's enabling time, REBUILT at the parameter vector θ. The world supplies whatever state context the distribution depends on internally; θ is the one input that varies, because the estimator calls this with a ForwardDiff.Dual-valued θ to differentiate the selection probabilities and the sojourn rate. The method must therefore build its return value arithmetically from θ — never capture a primal rate from the world — and return one concrete distribution type per eltype(θ) (the Exponential(one(eltype(θ)) / θ[i]) idiom), so the differentiated closures stay type-stable.\n\n\n\n\n\n","title":"ClockGradients.branch_clock_distribution"},{"category":"function","location":"reference/#ClockGradients.branch_state","page":"API reference","text":"branch_state(world)\n\nThe object handed to the user's f_state terminal functional. This is the model's physical/discrete state — a failure counter carried in it is how a cumulative count becomes a terminal-state read.\n\n\n\n\n\n","title":"ClockGradients.branch_state"},{"category":"function","location":"reference/#ClockGradients.check_branchable","page":"API reference","text":"check_branchable(world_factory, θ; nsteps=20, seed=0xC0FFEE) -> NamedTuple\n\nExercise the semantic obligations of the branchable-world protocol on worlds built by world_factory() and report the result. θ is the model's primal parameter vector, needed for the distribution probes (no protocol verb exposes the world's own parameters). Runs are capped at nsteps firings; seed derives every stream and rekey seed the harness uses, so the report is reproducible.\n\nThe returned NamedTuple has one Bool per obligation, an aggregate, and diagnostics:\n\npeek_repeatable — two consecutive peeks agree and are non-nothing.\npeek_commit_progress — committing the peeked firing advances branch_time to exactly the peeked time, and the following peek does not run backward.\nclone_coupled — a mid-trajectory branch_clone continues IDENTICALLY to the original: the two subsequent (time, key) sequences are equal, element for element.\nrekey_fresh_draw — after branch_rekey! on a clone, the very next peeked firing TIME differs from the un-rekeyed original's (fresh randomness must cover already-scheduled draws; equal continuous draws have probability zero).\nrekey_diverges — the rekeyed clone's whole continuation differs from the original's.\nrekey_couples — two clones rekeyed to the SAME seed produce identical continuations.\nforce_matches_commit — forcing the peeked (key, time) on a clone leaves the same time, the same branch_enabled_ages, and the same next peek as committing it naturally on the original.\nages_sorted, ages_nonnegative — at every step of a run, branch_enabled_ages comes back sorted by key with ages ≥ 0.\ndistribution_type — branch_clock_distribution returns a UnivariateDistribution for every enabled key at the primal θ.\ndistribution_dual — rebuilt at a ForwardDiff.Dual-valued θ, every enabled clock's distribution carries the dual in its parameters (Distributions.partype follows eltype(θ)), so derivatives can flow.\nschedule_consistent — when the world implements the OPTIONAL branch_schedule verb, its answer is truthful: sorted by time, keys exactly the enabled set, first entry agreeing with branch_peek. A world without the verb passes vacuously; the separate schedule_verb field reports :present or :absent.\npass — the conjunction of all of the above.\ndiagnostics::Vector{String} — one message per failed or errored check.\n\nA check that throws is reported as false with the exception in diagnostics — a world missing a verb entirely shows up as a MethodError there rather than aborting the harness.\n\n\n\n\n\n","title":"ClockGradients.check_branchable"},{"category":"function","location":"reference/#ClockGradients.capability_report","page":"API reference","text":"capability_report(model, θ; probe_horizon, probe_seeds) -> NamedTuple\n\nDiagnose which ESTIMATOR TIERS one model supports, in the style of check_branchable: one Bool per tier plus human-readable diagnostics, never an exception. The tier ladder is the design's grading of where gradient technology meets simulation expressiveness:\n\ntier0_simulate — everything the simulation framework can express. Never restricted by this package; confirmed by short probe simulations.\ntier1_replay_score — record replay and the score estimator (score_estimate). Requires the trajectory to determine the state sequence (no fire! may draw randomness) and an initial law the derived contract can fold from (and, for the score's initial term, a law that is θ-free or carries a log-density).\ntier2_pathwise_pairing — the pathwise/IPA replay (ipa_estimate) and the score/IPA pairing (paired_estimate). Additionally requires every clock distribution to be dual-safe — a member of DUAL_SAFE_DISTRIBUTIONS (Exponential, Weibull, LogNormal), the one source of truth the IPA replay gate also consults.\ntier3_branching — the branching and SPA estimators. Requires the clonable world (clone and rekey on a live simulation), a framework guarantee rather than a per-model property.\n\nTiers 0–2 are cumulative (a false tier falsifies the tiers above it); tier 3's requirement is independent of the record-replay requirements, so it is reported on its own. Each diagnostic names the RESPONSIBLE event family or model slot and one action that lifts the restriction; the unexercised field lists event families the probe never reached, whose obligations were therefore NOT checked (the tier booleans mean \"no obstruction detected\", not \"proved for every family\").\n\nThe core package defines NO methods: the tier checks read a framework's model value — its initial law, its per-family memory policies, its firing rules — so a framework's package extension attaches one method per model type it can diagnose (the core deliberately never names any framework, the same seam rule as gradient_record). See the extension method's docstring for its probe semantics (probe_horizon, probe_seeds) and the honesty caveat.\n\n\n\n\n\n","title":"ClockGradients.capability_report"},{"category":"function","location":"reference/#ClockGradients.branch_schedule","page":"API reference","text":"branch_schedule(world) -> Vector{Tuple{K,Float64}}\n\nOPTIONAL tenth verb: every currently-enabled clock paired with its SCHEDULED (putative) firing time, sorted by time, so the first entry is the same (key, time) pair branch_peek reports (transposed). Only the smoothed-perturbation-analysis estimator's TruncatedHazard() weight strategy requires it — the runner-up's residual η is its second entry's time minus the decision time — so a world may decline to implement it and still receive every other estimator, including SPA's default HazardWeight() strategy. A world without a method fails at first use with an ordinary MethodError naming this generic.\n\nImplementing it is cheap for a scheduling backend (the sampler already stores the putative times); a rejection-style backend that never schedules has nothing truthful to return and should leave the verb unimplemented rather than fabricate times.\n\n\n\n\n\n","title":"ClockGradients.branch_schedule"},{"category":"type","location":"reference/#ClockGradients.ClockWorld","page":"API reference","text":"ClockWorld(model, θ; seed) -> ClockWorld\n\nA ready-to-peek branchable world over a pure model-contract model: the model's initial_state with every initially-enabled clock scheduled at time zero from streams keyed by seed. Implements all branchable-world verbs (including the optional branch_schedule), so\n\nw = ClockWorld(model, θ; seed=1)\nbranching_gradient(() -> ClockWorld(model, θ; seed=1), θ, f_state; ...)\n\nis the one-line way to run the clone-based estimators on a model without a simulation framework. Distributions are frozen at each clock's enabling — see the file header for the state-dependence limitation.\n\n\n\n\n\n","title":"ClockGradients.ClockWorld"},{"category":"function","location":"reference/#ClockGradients.branching_gradient","page":"API reference","text":"branching_gradient(world_factory, θ, f_state; nreps, horizon, seed,\n                   branch_rng_seed, nparams=length(θ),\n                   max_branches_per_rep=nothing)\n\nThe Pflug / Hahn–Jordan weak-derivative branching estimator of ∂θ E[f_state(X_θ)], driven entirely through the nine branchable-world verbs — any framework whose world implements the protocol (certify with check_branchable) gets this estimator unchanged.\n\nMethodology. A generalized-semi-Markov trajectory factors, per event, into a sojourn law (how long until the next event, governed by the total hazard of the enabled set) and a selection law (which enabled clock fires next, the who-fires-next probability mass function). Differentiating the path expectation splits the same way (Pflug, eq. 4.52): a smooth time part — the score of the sojourn densities, whose parameter dependence is ordinary and low-variance — and a discrete selection part — the parameter sensitivity of the event-ORDER, which the pathwise/IPA estimator silently drops because an infinitesimal change of θ can flip which clock wins a race and IPA holds the realized order fixed. The selection part is recovered by the Hahn–Jordan decomposition of the pmf derivative dp = c(p⁺ − p⁻) into two probability vectors, drawing a p⁺ winner and a p⁻ winner, cloning the whole running world twice (branch_clone), imposing each drawn winner with branch_force!, continuing both clones to the horizon under COMMON random numbers (one shared branch_rekey! seed), and accumulating c·(f⁺ − f⁻).\n\nPrefer branching over pathwise/IPA exactly when the functional's derivative is carried by event ORDER rather than by event TIMES — count functionals, first- passage across a threshold, any observable that jumps at a reordering — the regime where IPA is biased. It costs two coupled clones per branch point, so it is the higher-variance, higher-cost member of the pair; the score/IPA pairing is the cheaper bias detector, and branching is the unbiased fallback it points to.\n\nArguments.\n\nworld_factory() returns an INITIALIZED, ready-to-peek branchable world built at params = θ (primal Float64). Each replication takes a fresh world from the factory and IMMEDIATELY rekeys it to its own derived seed (branch_rekey!), so the factory's embedded seed never correlates replications; the factory's INITIAL STATE, however, is shared by all replications (randomize it inside the factory if the model's initial state is itself random).\nθ is the primal parameter vector; the selection pmf and the sojourn densities are rebuilt at a dual θ through branch_clock_distribution.\nf_state(state) -> Real is the TERMINAL-state functional, read from branch_state. A cumulative count (e.g. number of failures) is expressed by carrying a counter in the state and reading it here — the machine-repair pattern — so the difference f⁺ − f⁻ cancels the shared clone prefix automatically.\n\nKeywords: nreps, horizon, seed (base-path master seed), branch_rng_seed (the estimator-owned p± draw master seed), nparams = length(θ), and max_branches_per_rep (see below).\n\nReturns a NamedTuple: estimate and stderr (per θ component), nreps, selection_part/selection_stderr and time_part/time_stderr (the two halves of the split, per component), fmean, and clones_per_rep.\n\nA world type that is missing one of the nine verbs fails with an ordinary MethodError naming the missing generic at its first use. The package extension for the sibling event-driven simulation framework supplies both the verb methods for its simulation type and a convenience method branching_gradient(sim_factory, initializer, θ, f_state; ...) that constructs and initializes the simulation per factory call.\n\n\n\n\n\n","title":"ClockGradients.branching_gradient"},{"category":"function","location":"reference/#ClockGradients.spa_gradient","page":"API reference","text":"spa_gradient(world_factory, model, θ, fn::PathFunctional;\n             nreps, horizon, seed, branch_rng_seed=seed+1,\n             strategy=HazardWeight(), jump_override=nothing,\n             fpt_budget=100_000)\n\nThe smoothed-perturbation-analysis (Fu & Hu) estimator of ∂θ E[fn(X_θ)]: the pathwise (IPA) term plus a hazard-weighted boundary term at event-order swaps and at the horizon, with each swap's jump estimated by a coupled DNP/PP clone pair driven through the branchable-world verbs.\n\nPrefer SPA exactly where the pathwise estimator is silently biased — count functionals, first passage across a contended threshold, any observable whose derivative is carried by event ORDER — and where the branching estimator would be the fallback: at comparable clone budgets SPA's per-epoch conditioning measured ≈5× tighter in variance×time than the Hahn–Jordan selection split on the machine-repair count. Validity requirements: dual-safe clock families (Exponential, Weibull, LogNormal — the replay carries dξ/dθ), a fire that is pure with value-== states (the commuting gate calls it speculatively), and NO mid-flight re-evaluation of an enabled clock's distribution (guarded: a multi-segment record throws).\n\nArguments mirror branching_gradient: world_factory() returns an initialized, ready-to-peek branchable world built at primal θ, and each replication immediately rekeys its world to a derived seed. model is the PURE model-contract twin of the law the world simulates — for a ClockWorld it is the same object the world was built from; for a framework world it is a parallel pure implementation, and a per-epoch audit throws if its enabled set ever disagrees with the live world's. fn is a PathFunctional; first passage runs to the hit (pass horizon=Inf) with fpt_budget capping each clone continuation.\n\nstrategy picks the weight form (see WeightStrategy); jump_override(model, s_pre, e_k, e) -> Float64 replaces the clone-pair jump with an analytic value where one is known (test instrumentation).\n\nReturns a NamedTuple: estimate/stderr per θ component, the ipa_part/boundary_part split with standard errors, and the cost counters clones_per_rep, candidates_per_rep, skip_fraction (criticality-gate skips over candidates), censored_frac (clone jumps lost to first-passage budgets), and discarded (first-passage replications whose base path never hit).\n\n\n\n\n\n","title":"ClockGradients.spa_gradient"},{"category":"type","location":"reference/#ClockGradients.WeightStrategy","page":"API reference","text":"WeightStrategy\n\nHow SPA's boundary candidates are enumerated and weighted.\n\nHazardWeight — every enabled non-winner is a candidate at each epoch, weighted by its hazard at its age. Needs only the required branchable verbs, and it is the COMPLETE form: an absorbing race has no \"next event\", so the single-pair strategy cannot express it. The default.\nTruncatedHazard — only the observed next event is a candidate, weighted by the truncated hazard f(ξ)/(F(ξ+η)−F(ξ)) with η the runner-up's residual. Several times fewer clones per replication but a wider standard error (a wall-clock tradeoff, not a dominance); requires the optional branch_schedule verb.\n\n\n\n\n\n","title":"ClockGradients.WeightStrategy"},{"category":"type","location":"reference/#ClockGradients.HazardWeight","page":"API reference","text":"HazardWeight()\n\nThe all-candidates SPA weight strategy: see WeightStrategy.\n\n\n\n\n\n","title":"ClockGradients.HazardWeight"},{"category":"type","location":"reference/#ClockGradients.TruncatedHazard","page":"API reference","text":"TruncatedHazard()\n\nThe single-pair SPA weight strategy: see WeightStrategy. Requires the world to implement the optional branch_schedule verb.\n\n\n\n\n\n","title":"ClockGradients.TruncatedHazard"},{"category":"section","location":"#ClockGradients.jl","page":"Home","text":"ClockGradients is the derivative-estimator layer for continuous-time discrete-event simulation. Given a stochastic model whose clocks race in continuous time — a generalized semi-Markov process (GSMP) — and a scalar observable f of its trajectory, the package estimates the derivative of the expected observable with respect to the model's parameter vector,\n\n∂θ E[f(X_θ)],\n\nby Monte Carlo. It sits above two sibling packages and consumes only their public surfaces:\n\nCompetingClocks.jl supplies the sampler and the trajectory record: the ordered firing sequence together with the survival-space uniform behind every firing, satisfying the retained-draw identity (see the CompetingClocks manual's \"Recording Trajectories\" and \"Contract and Invariants\" pages). Two of the three estimators here run entirely off that record, with no further access to the sampler.\nChronoSim.jl supplies the running world for the one estimator that needs more than a record: its \"How the system fits together\" chapter draws an architecture with an estimator-layer box outside both packages, and this package is that box. The branching estimator is written against a nine-verb branchable-world interface; ChronoSim's SimulationFSM implements those verbs (via its clone, rekey_streams!, and force_fire! capabilities — see ChronoSim's \"Cloning and branching\" page) through a package extension that loads only when ChronoSim is present, and any other framework can conform the same way.","title":"ClockGradients.jl"},{"category":"section","location":"#The-three-estimators","page":"Home","text":"The score-function estimator (also called the likelihood-ratio estimator) holds the trajectory fixed and differentiates its log-likelihood: ∂θ E[f] = E[f ⋅ ∂θ log L]. It is unbiased for every path functional, needs nothing but recorded trajectories, and pays for its generality with a variance that grows with path length.\n\nThe pathwise estimator, known in the simulation literature as infinitesimal perturbation analysis (IPA), holds the random draws and the event order fixed and differentiates the firing times themselves: ∂θ E[f] = E[∂θ f(X_θ)]. Where it is valid it is markedly lower-variance than the score, but it is only conditionally valid — exact when the functional is continuous in the firing times under the record's coupling, identically zero on functionals of the frozen discrete state, and silently wrong-signed when the functional's value depends on the event order itself.\n\nThe weak-derivative branching estimator (Pflug's method, via the Hahn–Jordan decomposition) recovers exactly the event-order sensitivity that IPA drops: at each race along a base path it clones the whole running simulation, forces a different winner in each clone, and differences the outcomes. It is unbiased for terminal-state functionals including counts, and it is the expensive member of the family — it requires a live branchable world rather than a record, and it spawns on the order of dozens of clones per replication.","title":"The three estimators"},{"category":"section","location":"#Which-one-should-I-reach-for?","page":"Home","text":"Run the score and pathwise estimators together on the same records with paired_estimate — the pairing is the package's default validation mode. The score estimate is a consistent estimate of the true derivative and the IPA estimate is a consistent estimate of its frozen-order part, so a statistically significant difference between them is a measurement of IPA's bias, and agreement is a certificate that the cheaper, tighter IPA number can be trusted. When the pairing flags a bias — which it does exactly on the functionals whose sensitivity lives in event order — fall back to the score estimate, or to branching_gradient when the score's variance on that functional is too large and the model exists as a live branchable world (ChronoSim via the extension, or any conforming framework). The manual page Choosing an estimator walks this decision with the package's own measured evidence.","title":"Which one should I reach for?"},{"category":"section","location":"#Manual","page":"Home","text":"Choosing an estimator — the decision guide: what each estimator needs, where each is valid, and the pairing methodology, with measured numbers.\nRecords and ingestion — the GradientRecord: building it from a TrajectoryRecorder or from a bare firing trace, the coupling label, the enabling-time audit, and segment chains for state-dependent models.\nThe branchable-world interface — the nine-verb protocol a framework implements to receive the branching estimator, with a worked adoption built on the raw sampler layer and the check_branchable conformance harness.\nWorked example — the machine-repair model end to end: model contract, simulation, score, IPA, the paired verdict, and the branching variant on a ChronoSim model.","title":"Manual"},{"category":"section","location":"#Reference","page":"Home","text":"Validity and invariants — the functional-class × estimator × coupling validity table, and the contract obligations the package enforces.\nAPI reference — docstrings for every exported name.","title":"Reference"},{"category":"section","location":"worked_example/#Worked-example:-machines-that-fail-and-get-repaired","page":"Worked example","text":"This page runs one model through the whole package: define it on the five-function model contract, simulate it through the real CompetingClocks sampler with a recorder attached, estimate a derivative with the score estimator, with the pathwise (infinitesimal-perturbation-analysis, IPA) estimator, and with the two paired, and read the verdict. The final section estimates the same derivative with the weak-derivative branching estimator on the same model written as a ChronoSim simulation. Every code block on this page executes during the documentation build; the printed outputs are real.\n\nThe model: n = 5 machines each fail independently at rate λ = θ[1] while up. Failed machines join a first-in-first-out queue served by a single repairman at rate μ = θ[2]. Because all clocks are exponential, the number of down machines is a birth–death continuous-time Markov chain (CTMC), which gives exact oracles to test every estimate against.","title":"Worked example: machines that fail and get repaired"},{"category":"section","location":"worked_example/#The-model-contract","page":"Worked example","text":"A model is five functions: initial_state, clockkeytype, enabled, clock_distribution, and fire. The parameter vector θ enters through clock_distribution only — that single seam is what lets the estimators re-evaluate distributions at a dual-valued θ without the sampler ever seeing a parameter.\n\nusing ClockGradients\nusing CompetingClocks: FirstReactionMethod\nusing Distributions\nusing ForwardDiff\nusing Random: Xoshiro\n\nimport ClockGradients: initial_state, clockkeytype, enabled, clock_distribution, fire\n\nstruct MachineRepair\n    nmachines::Int\nend\n\n# The state carries a cumulative failure counter so \"number of failures\" is a\n# pure state observable rather than a bespoke count-the-firings function.\nstruct MRState\n    up::Vector{Bool}\n    queue::Vector{Int}\n    nfail::Int\nend\n\ninitial_state(m::MachineRepair) = MRState(fill(true, m.nmachines), Int[], 0)\nclockkeytype(::MachineRepair) = Tuple{Symbol,Int}\n\n# Deterministic order: a Vector built by a fixed loop, never Dict/Set iteration.\nfunction enabled(m::MachineRepair, s::MRState)\n    ks = Tuple{Symbol,Int}[]\n    for i in 1:m.nmachines\n        s.up[i] && push!(ks, (:fail, i))\n    end\n    isempty(s.queue) || push!(ks, (:repair, s.queue[1]))\n    ks\nend\n\n# θ = [λ, μ]. Distributions.Exponential takes the MEAN, so rate λ is\n# Exponential(1/λ); `one(eltype(θ))` carries a dual θ's element type through.\nclock_distribution(::MachineRepair, θ, key::Tuple{Symbol,Int}) =\n    key[1] === :fail ? Exponential(one(eltype(θ)) / θ[1]) :\n                       Exponential(one(eltype(θ)) / θ[2])\n\n# Pure: returns a fresh state, never mutates its argument.\nfunction fire(::MachineRepair, s::MRState, key::Tuple{Symbol,Int})\n    up, queue, nfail = copy(s.up), copy(s.queue), s.nfail\n    kind, i = key\n    if kind === :fail\n        up[i] = false\n        push!(queue, i)\n        nfail += 1\n    else\n        popfirst!(queue)\n        up[i] = true\n    end\n    MRState(up, queue, nfail)\nend\n\nndown(s::MRState) = count(!, s.up)   # the downtime observable\n\nmodel = MachineRepair(5)\nθ0 = [0.5, 1.5]\nhorizon = 8.0\nnothing # hide","title":"The model contract"},{"category":"section","location":"worked_example/#The-exact-oracle","page":"Worked example","text":"The down-machine count is a birth–death CTMC with birth rate (n-k)λ and death rate μ for k > 0. Augmenting the Kolmogorov forward equations with an accumulator row gives the exact expected failure count (accumulate the total failure rate) or the exact expected integrated downtime (accumulate E[#down]), and writing the integrator generically in the element type lets ForwardDiff differentiate the oracle itself:\n\nfunction ctmc_oracle(λ, μ, n::Integer, T::Real; mode::Symbol, nsteps::Integer=2000)\n    S = promote_type(typeof(λ), typeof(μ), typeof(float(T)))\n    y = zeros(S, n + 2)          # p_0 … p_n plus the accumulator row\n    y[1] = one(S)\n    function rhs(y)\n        dy = zeros(S, n + 2)\n        for k in 0:n\n            fail = (n - k) * λ\n            rep = k > 0 ? μ : zero(S)\n            dy[k + 1] -= (fail + rep) * y[k + 1]\n            k < n && (dy[k + 2] += fail * y[k + 1])\n            k > 0 && (dy[k] += rep * y[k + 1])\n            dy[n + 2] += (mode === :failures ? fail : k) * y[k + 1]\n        end\n        dy\n    end\n    h = T / nsteps\n    for _ in 1:nsteps            # classic fourth-order Runge–Kutta\n        k1 = rhs(y); k2 = rhs(y .+ (h / 2) .* k1)\n        k3 = rhs(y .+ (h / 2) .* k2); k4 = rhs(y .+ h .* k3)\n        y .+= (h / 6) .* (k1 .+ 2 .* k2 .+ 2 .* k3 .+ k4)\n    end\n    y[n + 2]\nend\n\noracle_dfail = ForwardDiff.derivative(\n    λ -> ctmc_oracle(λ, θ0[2], 5, horizon; mode=:failures), θ0[1])\noracle_ddown = ForwardDiff.derivative(\n    λ -> ctmc_oracle(λ, θ0[2], 5, horizon; mode=:downtime), θ0[1])\n(dfailures_dλ = oracle_dfail, ddowntime_dλ = oracle_ddown)","title":"The exact oracle"},{"category":"section","location":"worked_example/#Simulate-and-record","page":"Worked example","text":"run_recorded drives the model through a real CompetingClocks.SamplingContext with a TrajectoryRecorder attached, and GradientRecord ingests the result (passing the enabling-time audit described in Records and ingestion). Every ingested firing satisfies the retained-draw identity:\n\nrec = run_recorded(Xoshiro(1), model, θ0, FirstReactionMethod(); horizon=horizon)\ngrec = GradientRecord(model, rec; coupling=:carry)\nreplayed = replay_times(model, θ0, grec)   # replay at θ0 must reproduce the record\n(nfirings = length(grec), max_replay_gap = maximum(abs.(replayed .- grec.time)))","title":"Simulate and record"},{"category":"section","location":"worked_example/#The-score-estimator","page":"Worked example","text":"The functional is declared once as a PathFunctional — here the terminal failure count — and simulate_and_estimate simulates, ingests, and averages f(X) ⋅ ∂θ log L. Component 1 is the λ derivative:\n\nfailcount = TerminalObservable(s -> Float64(s.nfail))\nsc = simulate_and_estimate(Xoshiro(90210), model, θ0, FirstReactionMethod(),\n                           failcount; nreps=6_000, horizon=horizon)\n(estimate = sc.estimate[1], stderr = sc.stderr[1],\n z_vs_oracle = (sc.estimate[1] - oracle_dfail) / sc.stderr[1],\n drift_alarm_z = sc.scoremean[1] / sc.scorestderr[1])\n\nThe estimate lands within the four-standard-error band of the exact CTMC derivative, and the drift alarm — the mean of the raw score, which is exactly zero in expectation — confirms the replay's bookkeeping agrees with the sampler that produced the records.","title":"The score estimator"},{"category":"section","location":"worked_example/#The-pathwise-estimator,-and-the-paired-verdict","page":"Worked example","text":"The failure count reads only the frozen discrete state, so its pathwise derivative is exactly zero on every path — the IPA failure mode. Running both estimators on the same records with paired_simulate_and_estimate turns that into a flagged verdict rather than a silent wrong answer:\n\npv_count = paired_simulate_and_estimate(Xoshiro(90210), model, θ0,\n    FirstReactionMethod(), failcount; nreps=6_000, horizon=horizon,\n    coupling=:carry)\n\nIPA is pinned at zero with zero variance; the score recovers the oracle; the difference is significant, so bias_detected flags the λ component (and the μ component). Now the same pairing on integrated downtime — a functional that is continuous in the firing times, where IPA is valid under the carry coupling these records store:\n\ndowntime = IntegratedOccupancy(ndown)\npv_down = paired_simulate_and_estimate(Xoshiro(2718), model, θ0,\n    FirstReactionMethod(), downtime; nreps=6_000, horizon=horizon,\n    coupling=:carry)\n\n(oracle = oracle_ddown,\n ipa_z_vs_oracle = (pv_down.ipa[1] - oracle_ddown) / pv_down.ipa_stderr[1],\n stderr_ratio_score_over_ipa = pv_down.score_stderr[1] / pv_down.ipa_stderr[1],\n bias_detected = pv_down.bias_detected[1])\n\nBoth estimators agree with the exact oracle, the pairing does not flag, and the IPA column is severalfold tighter than the score at the same sample size — agreement is the certificate that the cheap number is the one to report. (At 20,000 replications the package's test suite measures this as IPA 27.13 ± 0.075 versus score 27.30 ± 0.31 against the oracle 27.2216.)","title":"The pathwise estimator, and the paired verdict"},{"category":"section","location":"worked_example/#The-branching-estimator,-on-a-ChronoSim-model","page":"Worked example","text":"For the failure count — where IPA is identically zero and only the score's higher-variance estimate survives the pairing — the weak-derivative branching estimator gives an unbiased alternative. It needs a live simulation, not a record: the same machine-repair model is written as a ChronoSim model, with the failure counter carried in the physical state so the functional is a terminal-state read. The estimator itself only speaks the branchable-world interface; the ClockGradients–ChronoSim extension makes the SimulationFSM conform and supplies the (sim_factory, initializer) convenience method used below. (This mirrors the model in the package's test/test_branching.jl; see ChronoSim's manual for the event system and its \"Cloning and branching\" page for the capabilities the adapter maps.)\n\nmodule BranchRepairModel\n\nusing ChronoSim\nusing ChronoSim.ObservedState\nusing Distributions\n\nimport ChronoSim: precondition, generators, enable, fire!\n\nexport MRPhysical, repair_events, repair_initializer\n\n@keyedby Machine Int64 begin\n    up::Bool\nend\n\n# `nfail` is a plain passenger: no precondition reads it, so it generates no\n# clock; a clone copies it by value.\n@observedphysical MRPhysical begin\n    machine::ObservedVector{Machine,Member}\n    nfail::Int\nend\n\nfunction MRPhysical(n::Int)\n    m = ObservedArray{Machine,Member}(undef, n)\n    for i in 1:n\n        m[i] = Machine(false)\n    end\n    return MRPhysical(m, 0)\nend\n\nstruct Fail <: SimEvent\n    idx::Int\nend\n\n@guard precondition(evt::Fail, physical) = physical.machine[evt.idx].up\n\n@conditionsfor Fail begin\n    @reactto changed(machine[i].up) do physical\n        generate(Fail(i))\n    end\nend\n\n# The θ seam: failure rate λ = θ[1].\nenable(::Fail, physical, θ, when) = (Exponential(inv(θ[1])), when)\n\nfunction fire!(evt::Fail, physical, when, rng)\n    physical.machine[evt.idx].up = false\n    physical.nfail += 1\n    return nothing\nend\n\nstruct Repair <: SimEvent end\n\n@guard precondition(evt::Repair, physical) =\n    any(!physical.machine[i].up for i in eachindex(physical.machine))\n\n@conditionsfor Repair begin\n    @reactto changed(machine[i].up) do physical\n        generate(Repair())\n    end\nend\n\nenable(::Repair, physical, θ, when) = (Exponential(inv(θ[2])), when)\n\nfunction fire!(::Repair, physical, when, rng)\n    for i in eachindex(physical.machine)\n        if !physical.machine[i].up\n            physical.machine[i].up = true\n            return nothing\n        end\n    end\n    return nothing\nend\n\nrepair_events() = [Fail, Repair]\n\nfunction repair_initializer(physical, when, rng)\n    for i in eachindex(physical.machine)\n        physical.machine[i].up = true\n    end\n    return nothing\nend\n\nend # module BranchRepairModel\n\nusing ChronoSim: SimulationFSM\n\nsim_factory() = SimulationFSM(\n    BranchRepairModel.MRPhysical(5), BranchRepairModel.repair_events();\n    seed=UInt64(1), key_type=Tuple, params=θ0)\n\nres = branching_gradient(sim_factory, BranchRepairModel.repair_initializer,\n                         θ0, physical -> Float64(physical.nfail);\n                         nreps=300, horizon=horizon, seed=2026, branch_rng_seed=7)\noracle_dfail_μ = ForwardDiff.derivative(\n    m -> ctmc_oracle(θ0[1], m, 5, horizon; mode=:failures), θ0[2])\n(estimate = res.estimate, stderr = res.stderr,\n z_vs_oracle = [(res.estimate[1] - oracle_dfail) / res.stderr[1],\n                (res.estimate[2] - oracle_dfail_μ) / res.stderr[2]],\n clones_per_rep = res.clones_per_rep)\n\nBoth components of the gradient — the failure-rate derivative IPA pinned at zero, and the repair-rate derivative — match the differentiated CTMC oracle, at the price of roughly 76 clones per replication (the clones_per_rep field: two coupled clones per Hahn–Jordan branch). At 800 replications the same configuration measures z = [1.01, 0.04] against the oracle [10.727, 3.568], the package's exit criterion for this estimator — and the branchable-world interface page shows the same oracle reproduced through a world with no ChronoSim in it at all.","title":"The branching estimator, on a ChronoSim model"},{"category":"section","location":"worked_example/#What-to-take-away","page":"Worked example","text":"The workflow is always the same three calls: declare the functional, simulate and ingest records once, and run paired_estimate (or its simulate-and-estimate driver). The verdict tells you whether the tight IPA number is trustworthy; where it is not, the score estimate on the very same records is unbiased, and branching — the one estimator that needs the live world — covers the order-sensitive functionals whose score variance is too large. The full validity map, with the measured evidence, is the validity table.","title":"What to take away"},{"category":"section","location":"invariants/#Validity-and-invariants","page":"Validity and invariants","text":"This page is the contract half of the reference: first the validity table — which estimator is trustworthy on which class of path functional, under which coupling — then the obligations the package enforces and the ones it expects its callers to honor. The style follows the CompetingClocks manual's \"Contract and Invariants\" page: the manual pages show how to use the machinery; this page states what must always be true.","title":"Validity and invariants"},{"category":"section","location":"invariants/#The-validity-table","page":"Validity and invariants","text":"The functional's smoothness class is explicit in its type (IntegratedOccupancy, TerminalObservable, FirstPassageTime), and together with the record's coupling label it decides the validity of the pathwise (infinitesimal-perturbation-analysis, IPA) estimator. \"Coupling\" here means the rule for how the retained random draws are held fixed while θ moves; :carry retains each clock's enabling draw and preserves its age through re-evaluations, :redraw retains the last conditional draw (see Records and ingestion).\n\nFunctional class Score IPA, :carry record IPA, :redraw record Branching SPA\nIntegrated occupancy ∫₀ᵀ g(x_t) dt unbiased exact biased toward zero when re-evaluations occur not needed (API is terminal-state) valid; reduces to its IPA term\nTerminal discrete observable (counts, indicators) unbiased identically zero — 100% bias, zero variance identically zero unbiased unbiased, ≈5× tighter × time than branching\nFirst passage, hitting event order-stable unbiased exact (per-path) exact absent re-evaluations API is terminal-state valid; boundary term near zero\nFirst passage, hitting event contended unbiased wrong sign wrong sign API is terminal-state unbiased; recovers the sign\n\nSPA's cells hold only inside its stated requirements (see its obligations below): no mid-flight re-evaluation (guarded by a named error), a pure model twin, value-== states, dual-safe clock families.\n\nThe measured evidence behind each row, from this package's test suite (fixed seeds; oracles are exact continuous-time-Markov-chain (CTMC) forward-equation or closed-form values; \"flagged\" means the score/IPA pairing's four-standard-error test fired):\n\nIntegrated occupancy, plain machine repair (n = 5, λ = 0.5, μ = 1.5, T = 8, 20,000 replications): oracle 27.2216; carry IPA 27.13 ± 0.075; score 27.30 ± 0.31; pair z = 0.55, not flagged. IPA is roughly four times tighter than the score on the same records.\nIntegrated occupancy, load-dependent repair (state-dependent repair rate, so genuine mid-flight re-evaluations; 8,000 replications): oracle gradient [10.58, -4.93]; carry IPA within z = [-0.35, +0.26]; the redraw records built from the same trajectories give [6.58, -2.54] — 38–49% biased toward zero — and the pairing flags both components at z = [18.9, -23.4]. The score is unbiased under either coupling.\nTerminal discrete observables: the race win indicator (truth 2/9) and the machine-repair failure count (truth 10.727) both have IPA pinned at exactly 0.0 on every path; the score recovers each oracle; both flagged.\nOrder-stable first passage: on the two-clock race, the per-path IPA gradient of the minimum equals the hand derivative to 1e-9, and the 20,000-replication mean matches -1/(λa+λb)² = -1/9 (z = -2.35); the Weibull-versus-exponential race matches its quadrature oracle 0.30598 (z = -0.56).\nContended first passage (load-repair, first time three machines are down, 12,000 replications): the true repair-rate derivative is +0.583 — faster repair delays the hit, an event-order effect — while frozen-order IPA reports -0.220, the wrong sign, even on the occupancy-exact carry record; the pairing flags it at z = 27.5.\nTerminal count via branching (machine repair, 800 replications, ~76 clones each): estimate matches the oracle gradient [10.727, 3.568] at z = [1.01, 0.04] through the ChronoSim adapter and z = [0.46, 0.26] through the ChronoSim-free branchable test world, and agrees with this package's score estimator at pooled z = [1.24, 0.22].\nTerminal count via SPA (same regime, 1,500 replications): matches the oracle at z = [-0.55, -0.26] with a standard error 2.2–2.5× smaller than branching's at a comparable clone budget (the ≈5× variance×time advantage); the criticality gate skipped ~51% of candidate swaps without clones. Through the ChronoSim adapter with a hand-written pure twin, both weight strategies match the same oracle.\nContended first passage via SPA (plain machine repair, first time three machines are down, 2,000 replications): oracle [-8.13, +0.93]; SPA z = [-0.75, -0.25] while its own frozen-order IPA part reports the wrong-signed -0.24 on the repair rate — the boundary term restores the sign. The gate's first-passage form compares the swap's INTERMEDIATE states through the predicate; gating on state re-coalescence alone erases this entire correction.\nThe horizon boundary term (Poisson counting process, T = 3): with no order-swap candidates in existence, SPA reproduces dE[N(T)]/dλ = T exactly on every path (standard error at round-off) — the pin that finite-window counting needs the horizon term, not only swap terms.\n\nThe operational rule: the score column is always safe; the IPA columns are where the variance winnings are; and the pairing is how a particular (functional, coupling) cell is certified on your model rather than trusted from this table.","title":"The validity table"},{"category":"section","location":"invariants/#The-record's-contract:-the-retained-draw-identity","page":"Validity and invariants","text":"Every firing in a GradientRecord obeys\n\ntime[k] == te_k + invlogccdf(d_k, logu[k])\n\nwith te_k the firing clock's enabling time and d_k its distribution over the relevant segment. This is inherited from CompetingClocks, where it is a contract obligation on every TrajectoryRecorder firing (see the CompetingClocks manual's \"Contract and Invariants\" page), and it is what makes the record a sufficient statistic for both likelihood replay and pathwise replay. The package pins it two ways: ingestion runs the two-sided enabling-time audit (the bookkeeper's reconstructed te must equal the recorder's stamped te, exactly), and replay_times at the sampling parameter must reproduce the recorded times (pinned to 1e-9 across both sampler backends).","title":"The record's contract: the retained-draw identity"},{"category":"section","location":"invariants/#Coupling-labels-are-required-and-enforced","page":"Validity and invariants","text":"A record is constructed with coupling = :carry or coupling = :redraw; anything else is an ArgumentError. The label is not advisory: the two couplings retain different uniforms (the enabling draw versus the last conditional draw), so replaying one record through the other's recurrence would be silently wrong, and the replay entry points throw instead. :resume-style records — enable/disable pair chains where a clock's age survives a disabled gap — are out of scope in v0 and named as such in the error text.","title":"Coupling labels are required and enforced"},{"category":"section","location":"invariants/#The-carry-chain-replays-by-conditional-survival-pushforward","page":"Validity and invariants","text":"When a still-enabled clock's distribution changes at age a from d to d_new (a mid-flight re-evaluation, recorded as a segment boundary), the carry replay maps the clock's firing age af by matching conditional survival:\n\naf ← invlogccdf(d_new, logccdf(d_new, a) + logccdf(d, af) − logccdf(d, a))\n\nso the perturbed and unperturbed trajectories stay maximally coupled through the change. For exponential segments this reduces to the Gibson–Bruck rescaling (remaining time scales by the rate ratio), which the suite pins to 1e-12 against a hand-built two-segment trajectory. The states that rebuild each segment's distribution are θ-free: they are produced by folding the model's fire over the recorded keys, and only the distributions rebuilt from them through the four-argument clock_distribution carry ∂θ.","title":"The carry chain replays by conditional-survival pushforward"},{"category":"section","location":"invariants/#The-inversion-sampler-smoothness-requirement","page":"Validity and invariants","text":"Pathwise replay differentiates the map θ ↦ invlogccdf(d_θ, logu) at a fixed retained uniform. That map is only meaningful if the sampling rule is an inversion (quantile) rule: for a fixed uniform, the drawn value is a smooth function of the distribution's parameters. A rejection sampler has no such property — its accept/reject decision can flip under an infinitesimal parameter perturbation — so IPA-by-retained-draws requires inversion sampling, which is exactly what conditional_remaining implements and what the CompetingClocks recorder's logu coordinate inverts.","title":"The inversion-sampler smoothness requirement"},{"category":"section","location":"invariants/#The-dual-safe-family-set","page":"Validity and invariants","text":"Under a dual-valued θ (the ForwardDiff.Dual numbers that carry the gradient), every distribution the replay touches must have an invlogccdf with an analytic, differentiable implementation. That set is currently Exponential, Weibull, and LogNormal. A Gamma clock — and any other family whose quantile routes through the Rmath library's Float64-only code path — cannot flow a dual through, and rather than letting a MethodError surface from deep inside the automatic differentiation, the replay rejects it up front with a named error:\n\nusing ClockGradients\nusing Distributions\nimport ClockGradients: initial_state, clockkeytype, enabled, clock_distribution, fire\n\nstruct GammaClock end\ninitial_state(::GammaClock) = :on\nclockkeytype(::GammaClock) = Symbol\nenabled(::GammaClock, s::Symbol) = s === :on ? Symbol[:g] : Symbol[]\nclock_distribution(::GammaClock, θ, key::Symbol) = Gamma(2.0, θ[1])\nfire(::GammaClock, s::Symbol, key::Symbol) = :off\n\nrec = GradientRecord(GammaClock(), [1.0], [:g], [1.3], 10.0; coupling=:redraw)\ntry\n    ipa_gradient(GammaClock(), [1.0], rec, FirstPassageTime(s -> s === :off))\ncatch err\n    println(first(sprint(showerror, err), 250), \" …\")\nend\n\nThe score estimator has no such restriction — it differentiates log-densities, not quantiles — so a Gamma-clock model still gets score derivatives.","title":"The dual-safe family set"},{"category":"section","location":"invariants/#The-one-captured-record-closure-rule","page":"Validity and invariants","text":"The pathwise objective that gets differentiated is built by destructuring the lowered functional into plain local variables, so that the closure handed to the automatic-differentiation engine captures only the GradientRecord (plus the model). This discipline exists for the next engine, not the current one: v0 differentiates with ForwardDiff, which tolerates struct-field loads inside the differentiated region, but the reverse-mode engines this package is designed to admit later (the prototype phase measured Enzyme failing type analysis on exactly such loads) do not. Keeping the closure shape now means the adjoint path opens later without an API change.","title":"The one-captured-record closure rule"},{"category":"section","location":"invariants/#The-E[score]-0-drift-alarm","page":"Validity and invariants","text":"The expectation of the score ∂θ log L is exactly zero componentwise, so score_estimate reports the raw score's mean and standard error and the suite asserts |mean/stderr| < 4 on every component. This is the permanent alarm for bookkeeping drift between the simulator that produced the records and the replay that consumes them: a model edit, an enabling-rule change, or a record-construction bug shows up here before it corrupts a derivative estimate. It also justifies the control variate: subtracting the in-sample mean of f from the functional multiplies a mean-zero quantity, leaving the estimator unbiased while cutting its variance by orders of magnitude on functionals with a large mean.","title":"The E[score] = 0 drift alarm"},{"category":"section","location":"invariants/#Obligations-of-a-branchable-world","page":"Validity and invariants","text":"The branching estimator drives a live world through the nine-verb branchable-world interface, and its correctness leans on semantic obligations that no method signature expresses. They are stated in full in the verbs' docstrings and exercised by check_branchable; the three that silently corrupt estimates when violated:\n\nPeeking is non-committing and repeatable. Two consecutive branch_peek calls with no intervening mutation return the same (time, key) and leave the world's future unchanged — the estimator peeks every race before deciding whether to branch, and a mutating peek would advance the base path invisibly.\nEnabled ages come back sorted by key. The Hahn–Jordan probability vectors are built in branch_enabled_ages order and the drawn winners are indexed back into it, so a nondeterministic order desynchronizes the forced clone from the pmf that chose it.\nCoupled-clone and rekey semantics. branch_clone is a coupled copy (identical continuation until rekeyed, no perturbation of the original); branch_rekey! gives FRESH randomness including a redraw of already-scheduled firings (a resample at a stopping time, so the law is unchanged), with same-seed rekeys of two clones coupling them to each other. Re-seeding streams without redrawing scheduled clocks breaks replication independence: every replication would replay the factory world's opening firings.\n\ncheck_branchable returns per-obligation booleans with diagnostics rather than throwing, and the suite keeps negative controls (a mutating peek, an unsorted age table) proving each check has teeth. The OPTIONAL tenth verb branch_schedule is checked only when implemented (sorted by time, keys exactly the enabled set, first entry agreeing with the peek); its absence is conforming and reported as schedule_verb = :absent.","title":"Obligations of a branchable world"},{"category":"section","location":"invariants/#Obligations-of-the-SPA-estimator","page":"Validity and invariants","text":"spa_gradient adds four requirements beyond the branchable world, each enforced or stated:\n\nA pure model twin. The estimator's state logic — records, replay, criticality gates, jump construction, horizon terms — runs entirely on the five-function model, never on the live world's state object. The model handed in must be the exact twin of the law the world simulates; a per-epoch audit compares the twin's enabled set against the world's and throws a named error at the first disagreement.\nValue equality of states. The criticality gate decides zero jumps by comparing states after firing a pair in both orders; a state type whose == is identity (the default for structs with array fields) silently disables the gate — unbiased but clone-wasteful. Define == fieldwise.\nNo mid-flight re-evaluation. The boundary weight for a clock whose distribution was re-evaluated while enabled (its last-segment conditional law) has not been derived; a trajectory record carrying a multi-segment chain throws a named ArgumentError rather than average a wrong weight.\nThe tie construction. The DNP/PP jump pair fires two events at one instant through branch_force!; the regime is documented on the CompetingClocks contract page (the second force elapses zero time and triggers no keep-if-later redraw), and the exponential-race test pins the constructed jump at exactly the analytic ±1.","title":"Obligations of the SPA estimator"},{"category":"section","location":"invariants/#Obligations-inherited-from-the-model-contract","page":"Validity and invariants","text":"These are stated fully in the docstrings of the five contract functions (see the API reference); the two that silently corrupt estimates when violated are repeated here.\n\nDeterministic enabled order. enabled(model, state) must return keys in an order that depends only on the state value — a Vector built by a fixed loop, never Dict keys or Set elements. The record builder, the likelihood replay, and the pathwise replay all walk this iterable, and a nondeterministic order desynchronizes the replays from the sampler.\nθ enters through clock_distribution alone, and states are θ-free. fire is pure integer/boolean bookkeeping; enabled never reads θ; distribution methods must build their return value arithmetically from θ (never capture a primal rate from an enclosing scope) so that a dual-valued θ actually reaches the replay.","title":"Obligations inherited from the model contract"}]
}
